# 核心运行：零第三方依赖（Python 3.9+ 标准库即可直接跑通 demo 与全部测试）
#
# 仿真/视觉/二维码全功能可选依赖：
numpy>=1.24
opencv-python>=4.8
flask>=2.3
qrcode[pil]>=7.4
#
# 真机总线（STS3215）可选依赖：只有接实物舵机时才需要。不装也能 import
# （bus_sts3215.py 延迟导入 serial，缺库时给中文提示与安装命令）。
#   pip install -i https://mirrors.aliyun.com/pypi/simple/ pyserial
pyserial>=3.5
#
# Windows 仿真环境（另一台 4060 机器）建议：
#   pip install pybullet   # 或安装 Webots 后使用其 Python API
# ---------------------------------------------------------------------------
# CAD 建模依赖（不属于本包，勿装进主环境）
#   CadQuery + OpenCASCADE 仅供 design/cad/ 使用，见：
#       design/cad/requirements.txt
#   安装方式：python3 -m venv .venv-cad && #             .venv-cad/bin/pip install -r design/cad/requirements.txt
# ---------------------------------------------------------------------------
