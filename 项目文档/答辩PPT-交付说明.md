# A.T.R.I. 答辩 PPT 交付说明

> 交付日期：2026-09-11
> 版本：**定稿 v6（已通过子代理终审；随后按远端 design/ 新基线二次修订）**
>
> v6 触发原因：推送前 `git pull` 发现远端新增 8 个提交，其中
> `成本估算与质量困局核算` 与 `用真实元件参数校验设计模型` 推翻了本稿部分事实。
>
> 验收结论：
> - 可读性与吸引力：第一轮 6.5/10 → 第二轮 **7.9/10 通过**
> - 技术严谨性：第一轮 4/10（19 条阻断）→ 第二轮 6/10 → 第三轮 **7 项阻断全改，终审通过**
> - 版面几何自检：首轮 41 页压盖/越界 → **0 页**
> - 全片数值一致性自检：旧口径（1500 / 37.3 / 7.83 / 9.5 倍 / seed-0 收敛数字）残留 **0 处**

## 〇、v3.1 再交付（2026-09-11，架构对齐 v2）

> **触发**：数据口径切到协作者 **CAD 实装测量**（提交 6d4b077）——
> 实装包络 **418 mm**、结构件按真实体积实算 **1790 g**、整机 **3.437 kg**、踝关节 **111% ❌**。
> 构建器 `ppt/build_deck.py` 已同步并**重新出片**（51 页）。

| 项 | v3（本次之前） | **v3.1（本次）** | 出处 |
|---|---|---|---|
| 尺寸 | 47.5 × 19.0 × 11.0 cm | **41.8 × 22.3 × 12.9 cm**（CAD 实装） | `robot_model.json` v3 |
| 单侧臂长（肩→夹爪末端） | 12.25 cm | **9.57 cm** | `gen_urdf.shoulder_to_gripper_mm()` |
| 整机质量 | 2263 g（结构 552 + 舵机 1210 + 电子件 351 + 线束 150） | **3437 g**（结构 1790〔CAD 实算〕+ 舵机 1210 + 电子件与电池 316 + 线束 120） | CAD 实装 |
| 腿部关节力矩 | 1.152 N·m＝额定 1.0 的 115% | **1.629 N·m＝「堵转×50%」判据的 111% ❌** | `新架构参数总表.md` §6 |
| 减重路径 | — | **A 拓扑 / B 买金属件 / C 减自由度**（目标 73–90%） | 同上 §3.1 |
| 测试数 | 133 | **169** | `python3 -m unittest discover -s tests` |
| 预算三档 | ¥3105–3745 / ¥2755–3095 / ¥2935–3455 | **¥3350–3950 / ¥2725–3260 / ¥3100–3600** | `BOM与阶段性指标.md` |

**同时修掉的两处工具缺陷**（v3 交付说明第 3、4 节曾记录）：
1. `gen_urdf.py` 的"左臂(肩->夹爪)"标签**实际从骨盆起算**（77.0 mm），
   容易被误当材料口径的臂长 —— 现已改名为"骨盆->夹爪末端"，并**新增
   `shoulder_to_gripper_mm()`** 输出真值 95.7 mm。
2. `webots/tools/generate_atri_world.py` 的关节链是**手写常量**，长期与模型不一致
   （腿段 190 mm vs 模型 235 mm、roll/pitch 轴向对调）—— 现已改为**从 `robot_model.json` 派生**，
   世界里的质量与 URDF 同源。

> ℹ️ **PDF 暂不导出**（用户决定）：材料还会持续改动，等定稿后一次性导出即可。
> `ppt/out/ATRI-答辩PPT-定稿.pdf` 为**旧版**，与当前 PPTX 不一致，勿直接使用。

---

## 一、交付物

| 文件 | 说明 |
|---|---|
| `ppt/out/ATRI-答辩PPT-定稿.pptx` | **51 页**，含逐页转场（fade 700ms）与 45 页入场动画；用于修改 |
| `ppt/out/ATRI-答辩PPT-定稿.pdf` | 字体已**子集化内嵌**，用于现场播放——任何机器都不会跑版 |
| `ppt/out/pdf_p01.png … pdf_p51.png` | PowerPoint 真实渲染的逐页图，供快速核对 |
| `项目文档/答辩PPT-逐页文案-v3.md` | 全 51 页文案（本轮定稿文案） |
| `ppt/build_deck.py` | 全 51 页的**代码化构建器** |
| `ppt/design.py` / `ppt/diagrams.py` | 设计系统与自绘图表构件 |
| `ppt/animate.py` | 转场与入场动画（python-pptx 不支持，手写 DrawingML） |
| `ppt/check_layout.py` | 版面几何校验器（检出压盖/越界） |
| `ppt/build.sh` | 一键流水线：生成 → PowerPoint 导出 PDF → 栅格化 |

**现场怎么放**：直接用 PDF。它内嵌了思源黑体子集，不依赖任何字体安装。
要改内容再用 `.pptx`，那台机器需要装思源黑体 SC（Regular + Heavy 两个字重）。

## 二、请先看的六页

| 页 | 为什么 |
|---|---|
| **P02** 一页摘要 | 全片论点压缩；四个数字卡是"已经做出来的" |
| **P19** 22 关节编号图 | 用的是 `design/drawings/` 生成的真工程图，不是渲染图 |
| **P28** 视觉伺服 | **全片最强的一页**：技能层开环 vs 设计包真闭环，带收敛序列与鲁棒性边界 |
| **P31** 六项可执行产物 | 全片口径基准；后面所有「规划中」都建立在这六项之上 |
| **P47** 验证方法与指标 | 四项已出结果 + 六项写清怎么测 |
| **P51** 接下来 90 天 | 把愿景换成可验证承诺 |

## 三、设计规范（改版时请遵守）

**视觉主题**：工程蓝图 × 关节节点。浅底为主，章节页墨蓝满版。

**配色（8 个值，不要加新色）**

| 用途 | 值 |
|---|---|
| 墨蓝 章节页底/主标题 | `#0A2540` |
| 结构蓝 主色 | `#1264A3` |
| 蓝雾 卡片底 | `#E8F0F7` / 深一档 `#DCE9F4` |
| 浅蓝 次级线条 | `#9CC0DC` |
| 纸白 页面底 | `#F7F9FB` |
| 石墨 正文 | `#1F2A37` |
| 中性灰 次要说明 | `#6B7A8C` |
| 朱橙 **唯一强调色**（每页 ≤2 处） | `#E4572E` |
| 验证绿 状态语义 | `#2E9E7B` |

**字体**：`Source Han Sans SC Heavy`（标题/大数字）＋ `Source Han Sans SC`（正文）
—— 这是**两个独立字体家族名**，都要装。

**版式约定**：直角、细线、无阴影、无渐变、无圆角。每页固定：页眉 → 判断句标题 → 导语 →
细分隔线 → 正文 → 状态标签条 → 页脚。标题必须是**完整判断句**，不是名词短语。

**状态标签（每页只挂一个主状态）**：`● 已完成` / `◐ 进行中` / `○ 规划中` / `◇ 已设计未制造`

## 四、本轮（v3 → 定稿）改了什么

### 1. 两个子代理的评审结论
- 技术严谨性：第一轮 **4/10**（19 条阻断）→ 第二轮 **6/10**（不通过，收官问题）
- 可读性与吸引力：第一轮 **6.5/10** → 第二轮 **7.9/10**（**通过**）
- 产出：`项目文档/可读性与吸引力评审意见.md`、`-第二轮.md`

### 2. 最重要的一处翻案：把最强的证据藏起来了
`design/run_sim.py::servo_loop()` 是一个**真闭环**（含收敛出口、最大迭代、丢帧搜索、振荡降增益）。
v3 文案把它判为"阶段二待办"，等于自认没有量化证据。本轮实测复现：

```
偏移  5.0cm → 5 轮，末误差 0.240
偏移 12.0cm → 5 轮，末误差 0.930
偏移 20.0cm → 7 轮，末误差 0.850

sweep: ideal 12/12 ｜ nominal 12/12 ｜ harsh 9/12 ｜ adversarial 4/12
```

已写入 P28 / P31 / P43 / P47；P48 / P51 从「待办」改为「**待移植**」。

### 3. 事实基线更新（`git pull` 带来）〔v3 时点，已被上文 v3.1 取代〕
| 项 | 旧 | 新 |
|---|---|---|
| 尺寸 | 37.3 × 18.7 × 10.6 cm | **47.5 × 19.0 × 11.0 cm**（关节链几何累加） |
| 单侧臂长 | 未提 | **12.25 cm**（上限 30 cm） |
| 测试 | 51 项 | **133 项**（软件行为 51 + 设计模型/工程图 82） |
| 预算 | ¥1500–2800 | **全口径 ¥2788–3456 / 借用主控与已有电池后 ¥2100–2800** |
| 深度 | 11.0 cm | **12.3 cm**（足部几何前移 18mm） |
| 整机质量 | 「结构 1.65 kg + 舵机约 1.25 kg ≈ 2.9 kg」 | **2263 g**（结构 552 + 舵机 1210 + 电子件 351 + 线束紧固件 150）；超原设计预算 1650 g 的 **37.2%** |
| 腿部关节力矩 | 「尚未做静力学校核」 | **已核算：需 1.152 N·m，占 STS3215 额定 1.0 N·m 的 115%** |
| 机械配合 | 未核 | **舵机装不进关节壳 0/8 通过；电池放不下（105 > 94 mm）** |
| 仿真 | 未跑通 | Webots 22 DOF **零重力运动学世界** + 19 项离线桩测试 |
| 工程图 | 无 | 4 张带图号（ATRI-DWG-001/002/004/005） |

### 4. 主动降级的技术表述（8 处）
| 页 | 原表述 | 现表述 | 原因 |
|---|---|---|---|
| P34 | 特征比对 → 本地特征库 | **身份比对未实现**；`face_db.json` 从未被代码加载 | grep 零命中 |
| P36 | 视觉对齐 → 夹爪闭合 | **无目标检测器**，对齐空转 | 后端无 `detect_object` |
| P38 | 音乐播放、音舞同步 | **无音频播放代码**，仅关节轨迹 | 全仓无音频实现 |
| P24 | 支撑腿反相补偿 | 支撑腿 21/40 帧零位；躯干同相/半频 | 实跑 `generate_gait()` 反证 |
| P23/P41 | 新增任务不碰调度器 | **仍需改一行白名单** | `VALID_SKILLS` 是冻结集合 |
| P06/P49 | 千元级 | **三千元级**（BOM 逐项求和） | 12 行合计 ¥3105–3745 |
| P28 | 视觉伺服闭环 | 技能层开环；**闭环已实现于设计包，待移植** | 见上 |
| P30/P47/P48 | Webots 仿真能验行走 | **零重力世界不承担"行走"指标** | `WorldInfo.gravity 0` |

### 5. 结构性调整
- 新增 **P31「已完成清单」锚页**（六项真能执行的产物 + 命令）
- 新增 **P46「答辩 Q&A 预判」页**
- P45 删除并入 P15；P17+P18 合并
- 五项任务页（P34–P38）加**完成度条**（1/4、3/5、2/6、2/5、2/5），避免连翻五页只看到一排空心圆

### 6. 动画与转场 —— ⚠️ 只保留转场，入场动画已移除
- **转场**：全 51 页统一 `fade` 700ms——答辩场合要平稳，不用推拉擦除
- **入场动画：已全部移除。** 手写 DrawingML `<p:timing>` 实现的逐元素淡入，
  在页数少时 PowerPoint 能凑合打开，但 45 页 × 每页若干形状（约 3200 个 cTn 节点）时
  PowerPoint **直接拒绝打开文件**。补 `bldLst` 后仍未解决。
  二分定位结论：**无动画的 51 页可正常打开，加动画即失败**。
  交付物打不开是致命的，入场动画只是锦上添花，故只保留转场。
  若需要入场动画，请在 PowerPoint 里手动添加（可框选多页一次设置）。
  `ppt/animate.py` 的 `add_entrance` 保留但不再调用，供后续排查。

### 7. 版面几何校验
写了 `ppt/check_layout.py`，扫描全部 51 页的文本框压盖与越界。
首轮 **41 页有问题** → 现 **0 页**。

## 五、需要你决定 / 需要别人做的事（我没动的部分）

### A. 设计数据包里有三处判断，我判断属于他人工作区，没有修改
本会话期间另一个 agent 在该仓库提交了 7 个 commit（`design/`、`webots/`），因此我没有改动这些文件，只把问题原样报给你：

1. **`design/robot_model.json` 的 `design_constraints`**
   - `competition_max_depth_mm: 300.0` —— 官方六类约束里**没有"深度 ≤30cm"这一条**（官方是身高/体宽/**臂长(单侧)**）
   - `single_arm_max_length_mm: 300.0` —— **声明了但 `gen_urdf.check()` 从未校验**
   - 建议：删或改名深度约束；把臂长真正纳入 `check()`

2. **`design/drawings/01_关节编号图.svg` 的「尺寸合规核验」框**
   - 只核了高度/体宽/深度，**4 张图里"臂长"出现 0 次**，却写着"可直接用于报名与检录核对"
   - 本 PPT 的 P19 **刻意裁掉了这个框**并加了说明；但图纸本身若作为材料提交，建议重出

3. **质量预算未含舵机 / 扭矩口径**
   - `robot_model.json` 质量合计 1.650 kg，但 23 个 link 里**没有舵机**；22 只 STS3215 级舵机约 +1.25 kg
   - `effort_nm` 有 8 路 > 舵机额定 2.0 N·m，其中 4 路 = 堵转 3.0 N·m，且 `check()` 不校验扭矩
   - `design/packages/domain_random.json` 的 `mass_kg` 范围 `[1.55, 1.75]` 会随之错位

4. **`gen_urdf.py::reach_mm` 的标签**
   - 它从骨盆开始累加，输出 78.3 mm，标签却写"肩→夹爪"；真正的肩→夹爪约 **122.5 mm**
   - 本 PPT 用的是 122.5 mm（12.25 cm），工具的标签建议一并修

5. **`webots/README.md`** 说零行程关节是 `left/right_hip_yaw` + `hip_roll`，实测 `left_hip_yaw` 有 0.75° 行程（`kick.py` 下发的）

### B. 现场演示的建议（我在 PPT 里留了位）
两位评审都指出：**51 页里没有一页呈现"一台机器人在走"**。
- 借到同构双足平台拍 10 秒真机视频插进 P31 / P39 —— 性价比高于继续打磨任何一页文案
- 给 Webots 世界加上重力（`gravity -9.81` + 地面接触），哪怕只能站 2 秒，P47/P48 就从最危险的页变成最有力的页
