# 线程报告：T-04 踢球评测（S1，2026-09-13）

> 对齐 T-03 证据链。测的是代码与几何，不是实物。
> 未测项 B7（实机踢球成功率 ≥70%）**本轮不闭合**。
> 禁止 git；只碰本线程独占文件。

---

## 1. 做了什么

| 文件 | 动作 |
|---|---|
| `software/atri/config/kick.json` | 新建。纯说明，不覆盖任何键 |
| `software/atri/atri/skills/kick.py` | 只加 `load_tuning` + `param_or` 薄封装；默认 1 cm / 5 轮 / 0.5 / 10° / 4–40 cm、不收敛也踢 |
| `software/atri/tools/kick_eval.py` | 新建。无 numpy/cv2 时 A 段 skip，B 段走一维几何 + 真 `KickSkill` |
| `software/atri/tests/test_kick_eval.py` | 新建。锁「无球不下发踢」「死区内 0 次迭代」 |
| `design/handoff/T-04-踢球-方案与评测报告.md` | 由评测工具一次生成 |

优先级：任务卡 params > `kick.json` > 代码默认。gain 模型与 T-03 相同（`x_rel = x_true − D·tanθ`），**未标定**。

## 2. 环境

- `ls /Users/zhangjingkun/Projects/github/ATRI/.venv-face/bin/python` → 不存在（exit 1）
- 系统 `python3` 3.9.6，无 numpy / 无 cv2
- 未用 `.venv-cad`，未装系统包
- 因此 A 段（合成绿球 + 真 `detect_ball`）**未跑、缺依赖**；B/C 段几何闭环 + 边界表已跑

## 3. 关键数字（几何口径，不是实机）

| 项 | 数字 | 能写进材料？ |
|---|---|---|
| A 绿球检测 | 未跑（缺 cv2） | 不能外推成检出率 |
| B 代码默认 | 技能 ok **20/24（83.3%）**，闭环收敛 **11/24（45.8%）** | 能写「几何闭环」，不能写成踢球成功率 |
| B 推荐参数 `max_iters=15, step_gain=1.0` | 技能 ok **20/24**，收敛 **20/24（83.3%）** | 同上；gain 未标定，不能上实机 |
| B 失败原因 | 两组各 4 次「出画 / 球未检出」，**不下发踢** | 能 |
| 15 cm 物距默认可纠正 | **2 cm**（死区 1 cm、5 轮） | 调参依据 |
| 15 cm 推荐可纠正 | **29.3 cm**（覆盖视场 ±8.66 cm） | 调参依据，未标定 |
| 实机踢球成功率 | **未测** | 不能 |

读法：踢球**不收敛也踢**，所以「技能 ok」高于「闭环收敛」。把 20/24 写成赛题 ≥70% 是口径错误。

## 4. 验收命令与真实输出

每条命令单独跑，后面跟 `echo "exit=$?"`，禁止 `A && B`。

### 4.1 冒烟：brain + kick_eval

```
$ cd /Users/zhangjingkun/Projects/github/ATRI/software/atri
$ python3 -m unittest tests.test_brain tests.test_kick_eval -v
...
test_kick (tests.test_brain.TestBrain) ... ok
test_kick_converges_in_simulation (tests.test_brain.TestServoClosedLoop) ... ok
test_static_observation_hits_iteration_cap_and_still_kicks ... ok
test_kick_out_of_range_distance_fails ... ok
test_kick_with_found_false_does_not_kick ... ok
test_no_ball_fails_without_kicking (tests.test_kick_eval.TestKickSkillLocks) ... ok
test_deadband_zero_iterations_still_kicks ... ok
test_image_scan_skips_without_cv2 ... ok
...
Ran 75 tests in 1.203s

OK
$ echo "exit=$?"
exit=0
```

### 4.2 全量 discover

```
$ python3 -m unittest discover -s tests
...
----------------------------------------------------------------------
Ran 624 tests in 46.845s

OK (skipped=45)
$ echo "exit=$?"
exit=0
```

（skip 45 是本机无 opencv/人脸依赖的既有跳过，不是本线程新红。）

### 4.3 评测脚本

```
$ python3 tools/kick_eval.py --report /Users/zhangjingkun/Projects/github/ATRI/design/handoff/T-04-踢球-方案与评测报告.md
================================================================
T-04 踢球评测：A 绿球检测 / B 闭环 / C 参数边界
================================================================
numpy=无  cv2=无
参数出处：kick: 有 kick.json 但未覆盖任何键 → 全部为代码默认值
口径：本机无 `.venv-face`、系统 python 无 numpy/cv2：**几何闭环 + 边界表已跑**；A 段（合成绿球图）未跑、缺依赖。未装系统包。
[A] 跳过：缺 numpy/cv2，A 段未跑
[B] 闭环：代码默认 ok 20/24 收敛 11/24、推荐参数 ok 20/24 收敛 20/24（模式 geometry）
[C] 参数边界表已算出（§4）
报告 → .../design/handoff/T-04-踢球-方案与评测报告.md
$ echo "exit=$?"
exit=0
```

## 5. 未做 / 不要写进材料

- 未闭合 B7
- 未跑合成绿球 `detect_ball`（缺 `.venv-face`）
- 未标定 gain / 焦距 / 摆腿接触
- 未改 `test_brain.py`、未测项清单、CAD、其它技能
