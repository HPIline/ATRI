# 暂停与恢复（2026-09-12，为加载 DSH 插件重启做准备）

> **恢复入口**：重启后让主会话先读这一份，再读 `design/handoff/第11轮-多线程施工方案与Prompt.md`（本轮施工方案与可粘贴 prompt）。
> 本文件只记录"暂停那一刻的真实状态"，不放计划以外的东西。

## 一、暂停时的仓库状态

| 项 | 值 |
|---|---|
| HEAD | `d254ee1`（力矩力臂口径修正；已推送，与 `origin/main` 一致）|
| 工作树 | **干净**（无未提交改动、无未跟踪文件）|
| 半成品 | 已收进 `git stash`：`stash@{0}` = `暂停前在跑线程的半成品（背挂衔接 skeleton/assembly + 预览冲突可视化 preview）` |
| 补丁副本（防 stash 意外） | `/Users/zhangjingkun/Projects/scratch/atri-pause-wip-20260912.patch`（856 行）|
| 恢复方式 | `git stash pop`（或 `git apply <补丁>`）；**恢复后必须重新验收**，那是未验证的半成品 |

**被中断的两条线程与进度**（都**没有**交付报告，改动都**未验证**）：

1. **背挂衔接**（目标：把 `连通分量 2 → 1`；背板离躯干框 3.00 mm 无接触面）
   半成品：`design/cad/skeleton.py` +105 行、`design/cad/assembly.py` +9 行。
2. **预览冲突可视化**（目标：按判定分档着色 + 图例 + 冲突隔离/点选）
   半成品：`design/cad/preview.py` +511 行。
   ⚠️ HEAD 里的 `preview.py` 是**已验证**的 6.37 MB 版本（22 滑条/5 预设/配合件半透明），回退到 HEAD 不会丢东西。

## 二、恢复后的队列（按顺序，重 CAD 一次只开一条）

| # | 事情 | 归属文件 | 备注 |
|---|---|---|---|
| ① | **重生成 `placements.json`** + 全量复验 `❌=0` | `design/gen_placements.py`（只跑）→ `design/placements.json` | 会把 **RPi 从头里挪走**、IMU 移到骨盆、相机罩轴向修正；会动装配基准，必须重跑 6 条验收 |
| ② | **管-舵机族让位**（12 对 × ~2360 mm³，最大干涉族）| `design/cad/skeleton.py` 的 `limb_tube()` | 推荐**管壁四角开让位槽**（不动 Φ40 接口、不加宽整机）；需先决策，但**不必等仿真** |
| ③ | **T2 姿态扫掠 → 关节限位** | `design/cad/sweep_check.py` | prompt 在施工方案 §三 T2；顺带为预览 5 个预设做碰撞校核 |
| ④ | **修 `torso_frame` 三实体**（非单一可打印体）| `design/cad/skeleton.py` | 与 ② 同文件，可同批做 |
| ⑤ | **全量 `build.sh`**：STEP/STL/工程图/渲染/预览一次重出 | `design/cad/build.sh` | T1/T3 完成后产物才会一致 |
| ⑥ | 力矩**两口径口径模板**（标"待队友仿真复核"）| 文档 | 数字已在 `hardware_requirements.json` 的 `torque_check` |
| ⑦ | 背挂衔接 / 预览冲突可视化 | 见 §一 | 从 stash 恢复半成品继续，或按原 prompt 重派 |

**重派线程用的 prompt 都在** `design/handoff/第11轮-多线程施工方案与Prompt.md`（§三 有 T1/T2/T3/T4/T6 可直接复制；§六 是减重与材料结论与红线）。

## 三、当前已知问题清单（都是实测）

| # | 问题 | 量级/证据 |
|---|---|---|
| 1 | 背挂那簇与机体脱开 | 背板 ↔ 躯干框 **3.00 mm**；`连通分量 2`（应为 1）|
| 2 | `torso_frame` 非单一可打印体 | 3 个互不相连实体 |
| 3 | `placements.json` 过期 | RPi 仍在头部（造成 top1 干涉 4224 mm³）、IMU 未到骨盆、相机罩轴向未修 |
| 4 | Φ40 管里塞 45.2 mm 舵机 | **12 对** × ~2360 mm³（占干涉总量 18%）；四角穿出管壁 |
| 5 | 叉 ↔ 相邻关节笼 | ≥4 对 × 2207 mm³（21%）|
| 6 | 头颈簇拥挤 | `head_yaw` 笼↔RPi 4224、`head_pitch`↔相机 3556、yaw 笼/舵机↔pitch 舵机 3512/3226 |
| 7 | 髋偏航笼 ↔ 手腕舵机 | 2 对 × 2297 mm³（**姿态相关**，零位手臂贴骨盆）|
| 8 | 力矩主口径未定 | 零姿态 41.1% / 最不利 45.1%（`trunk_roll`）；10 个腿部关节仍超额定 |

> 口径提醒（写材料时必须带）：`❌ 摆放错误 = 0 对` **不等于"没有穿模"**；总量 189 对 / 159 743 mm³ 里，
> 绝大多数是**同关节配合面**（50 件，预览已半透明）与 ⚠️ 级让位不足（多为自身体积 12–20%）。

## 四、复验命令（每次线程交付后都跑这套）

```bash
.venv-cad/bin/python design/cad/assembly.py --all --no-export      # 86/86、摆放错误 0 对
.venv-cad/bin/python design/cad/audit_assembly.py --top 12         # 轴对齐 22/22、连通分量 1、看有无新增大对
.venv-cad/bin/python design/cad/test_layout.py                     # 17 项全绿
.venv-cad/bin/python design/cad/pair_inspect.py --errors           # 必须为空
.venv-cad/bin/python design/cad/build_all.py --all                 # 质量不上升 + 几何列无 ❌
cd software/atri && .venv/bin/python -m unittest discover -s tests # 437 项 OK
python3 -m unittest discover -s webots/tests                       # 40 项 OK
```
数字任何一项变差 → 不进提交，先查因。**防"假绿"**：每条命令后显式 `echo "exit=$?"`，不用 `A && B`。

## 五、等用户拍板的事

1. 力矩**主判据口径**（零姿态静力 / 限位内最不利）——用户已定"等队友仿真，材料里先按乐观估计"。
2. 财务报表、知识产权清单两个 **xlsx 模板**（用户从平台下载后给我）。
3. 计划书格式要求、视频时长要求。
4. 是否开并行轻活：`software/atri` 侧 bringup/T-03 增强、精确"零件族→link"质量映射、视频路线 B 渲染动画脚本。
