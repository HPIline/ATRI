# 装配一致性修正记录（方向 / 穿模 / 未衔接）

## 第 6 轮（2026-09-12，`atri-next`）

按静立与赛框审查后的方案改布置，**不切 AABB 当补丁、不加外观壳、不改 `kit.IF` / stagger**。

做了：

- `pelvis_frame` 改 U 形：材料走 hip_yaw 外侧（y=±60）和后桥（x=−36），胯口朝 +X。`hip_yaw` |Y| 仍为 45 mm。
- IMU 从 (0,0,+9.5) 挪到后桥上表面，避开 `trunk_roll` 机体（该机体 z≈7–32）。
- `head_shell` 地板开大、不抬 z0；拾音模块挪到前脸外侧 (42,0,−8)。
- 肘叉改紧凑且无芯棒（前臂 39.2 mm 不够同时塞标准叉和夹爪舵机）。
- `gripper_jaw` 改锁在舵盘外表面，指身沿 −Z，避免零位伸进髋。
- `foot_plate` 后跟 48 mm（板长 124）、2 纵梁 + 前后横梁、四角 φ8×2 垫、前掌倒角；底板不打穿。

门禁：`design/cad/test_layout.py` 全绿。实装包络 **407 × 263 × 149 mm**（深 +3 mm，拾音前伸；宽仍 263，赛框 300 还剩 37 mm）。头顶 zmax 未升。

本轮清掉的 ❌：肘叉×夹爪舵机、夹爪×自身舵机、头舵机×拾音、腰舵机×IMU。

仍留（不在本轮范围，禁止用 pair_inspect 切盒修）：

- 背包板 × XL4015（电子件贴板，体积阈值误伤）
- `cluster_horn_arm` × hip_yaw 笼/舵机（簇臂布置，下一轮）
- 右夹爪 × 右 hip_yaw（猿肩短臂，零位手在髋旁）
- 足叉 × 踝舵机（叉锁舵盘是配合，体积 ≥5000 被判 ❌）

扭矩口径未改（质量方案未定案，不回灌 URDF）。

---

> 修正日期：2026-09-11
> 触发：交互预览（`design/cad/out/preview/ATRI-preview.html`）中出现**结构方向不对、穿模、未衔接**
> 范围：**只修装配一致性错误**（方向/干涉/连接）；**不做形体细化**——重量方案尚在讨论中，
> 现在细化会被推翻。细化项一律留到质量路线定案后。
> 工具：`design/cad/audit_assembly.py`（体检，出数字）、`design/cad/tools/render_assembly.py`（出图，肉眼复核）

---

## 0. 结论摘要

| 指标 | 修正前 | 修正后 |
|---|---|---|
| 相交体积合计 | **741 740 mm³** | **545 256 mm³**（−26%） |
| 干涉件对数（>1 mm³） | 320 对 | 241 对 |
| **舵机轴向 ↔ 关节轴一致** | yaw 类整整差 90°，共 22 个关节中多处不符 | **22 / 22 ✅** |
| 单个零件内部"飘块" | `joint_cage` 有 3 块独立实体 | **0（全部零件单实体且 `isValid()`）** |

剩下 545 256 mm³ **不是同一类问题**，见第 4 节分类：绝大部分属于
**躯干/头部容积超额**（等重量方案定案再摆）与**关节布局**（相邻舵机机体位置），
不属于"建模错误"。

---

## 1. 根因（三条，都是系统性错误）

### 1.1 舵机占位体比 `orient()` 的约定差 90°（影响全部 22 个关节）

`assembly.servo_placeholder()` 按"输出轴沿 **+Z**"建模（Φ20 盘放在 ±Z），
而 `kit.orient()` 的约定是**局部 +Y → 输出轴、+Z → 母端**。
两者差 90° ⇒ 每个关节的舵机都横躺着，机体的 45.2 长边顶进相邻结构里
（这就是预览里"到处穿模、方向怪"的主因）。

### 1.2 `orient()` 本身不可靠（yaw 类方向对映射错误）

原实现用 `gp_Trsf.SetTransformation(gp_Ax3(from), gp_Ax3(to))`。
实测（标记块法）发现：当 `(shaft, parent)` 取到"轴与母端分属不同坐标轴"的某些组合时，
**局部 +Y 并没有走到 shaft 上**——例如 `shaft=-z, parent=+y` 时局部 +Y 落在世界 Y 上。
`hip_yaw` / `head_yaw` 正是这一组合（轴与母端同轴，代码用"取一个垂直方向当母端"的退化处理，
于是撞上这个 bug），所以 **yaw 关节的舵机整整歪 90°**。

**已重写为显式右手正交矩阵**，映射关系可逐条验证：

```
局部 +Y → shaft        局部 +Z → parent        局部 +X = shaft × parent
```

### 1.3 舵机包络的 35 与 24.7 用反 + 安装孔开在了错误的板

`skeleton.py` 里：

```python
L_S, W_S, H_S = S["body_mm"]      # 45.2 / 24.7 / 35.0
GAP = W_S + 0.5                   # 25.2 ← 两侧板净距，但沿轴厚度是 35.0
AXIS_DZ = servo_axis_offset_mm()  # 6.5  ← 轴线其实在宽度方向居中
Z_TOP = H_S/2 - AXIS_DZ           # +11.0 ← 应为 +12.35
Z_BOT = -H_S/2 - AXIS_DZ          # −24.0 ← 应为 −12.35
```

- 官方与实测一致：**35.0 是沿输出轴的厚度**，垂直轴截面是 45.2 × 24.7；
- 且**轴心不在长度中点**：距 +X 端面 10.2、距 −X 端面 35.0；
- 4 个 M2.5 安装孔在**两个轴向端面**上（方形 9.9×9.9 = 节圆 Φ14），
  原代码却把它们钻在**底板**上（"从底部往上拧"），所以笼子实际上没有跟舵机连上。

### 1.4 附带发现的零件内"飘块"（预览里的碎料）

- `joint_cage`：插接芯棒写成 `z_cap_bot + wall + 2.0`，凭空留了 2 mm 空气缝
  → Φ34 芯棒成为**独立实体**；减重窗高 30 且越过顶板 → 侧板被切成**两块 73 mm³ 碎料**。
  合计 4 个实体（应为 1）。
- `joint_cage_yaw`：角柱高度按 `2*z_sec` 算（假设两端对称），
  修正轴向尺寸链后两端不再对称 → 角柱短 3.5 mm，够不到端板。

---

## 2. 改了什么（逐文件）

| 文件 | 改动 |
|---|---|
| `design/cad/kit.py` | 新增 **`servo_frame()`**：舵机标准姿态的唯一真值来源（含轴心偏心 10.2、凸台 2.5、花键 1.5、副轴 4.1、安装孔阵 9.9）。**重写 `orient()`** 为显式正交矩阵。删除错误的 `servo_axis_offset_mm()`（6.5 那套） |
| `design/cad/skeleton.py` | 常量全部改走 `servo_frame()`：`GAP=35.5`、`PLATE_Y=19.25`、`Z_TOP/Z_BOT=±12.35`、`SPAN_X=53.2`、`HORN_FACE=24.0`。`joint_cage`：安装孔改为**沿轴穿过两块侧板**；芯棒坐到顶板上；减重窗避开孔带。`joint_cage_yaw`：两端面位置按真实尺寸链（−20.0 / +17.5）、角柱高度按实际跨度、新增 4×M2.5 安装孔。`limb_fork`：**删除与舵机副轴同轴的"臂 B + Φ5.96 轴颈"**（二者不可能共用一条轴线），臂 A 内表面改用真实舵盘面 +24.0。`compact_adapter`：安装孔改沿轴钻、副轴侧轴承移到 −Y 板、+Y 板补 Φ26 凸台避空 |
| `design/cad/assembly.py` | `servo_placeholder()` 按 `servo_frame()` 重建于标准姿态（输出 +Y），并补上 Φ20 凸台、Φ5.9 花键、Φ6×4.1 副轴 |
| `design/cad/audit_assembly.py` | **新增**：穿模（相交体积）+ 未衔接（距离图/连通分量）+ **关节轴对齐**三项体检，可复现 |
| `design/cad/tools/render_assembly.py` | **新增**：整机多视角 PNG 出图，供肉眼复核 |

### 2.1 载荷路径的一处澄清（不是改设计，是修错误）

`limb_fork` 原来有一根 Φ5.96 轴颈插进笼子里的轴承内圈，但**舵机副轴 Φ6 与之同轴**，
两者不可能同时占据同一条轴线。按实物事实（副轴随输出转动）确定：
**第二支点 = 笼子 −Y 板的 MF106ZZ 骑在舵机副轴上**，叉子不再做轴颈。
载荷路径：舵盘（臂 A）→ 底座 → 子端插接芯棒。

---

## 3. 怎么复现与验收

```bash
cd <repo>   # 仓库根目录

# ① 零件 → 装配 → 预览（一键）
bash design/cad/build.sh --fast

# ② 装配体检（穿模体积 / 连通性 / 关节轴对齐）
.venv-cad/bin/python design/cad/audit_assembly.py --top 25

# ③ 出图肉眼复核
.venv-cad/bin/python design/cad/tools/render_assembly.py --views iso,front,side
```

**验收线（建议纳入后续回归）**：
- 关节轴对齐 **22/22**（低于 22 就是又转错了）；
- 每个零件 **单实体 + `isValid()`**（多实体 = 有飘块）；
- 干涉对里**不允许出现**"舵机—舵机（不相邻关节）""同一模块内部件互相咬"这类；
  允许存在的只有第 4 节列出的已知容积冲突。

---

## 4. 剩余干涉分类（修正后 545 256 mm³）

### 4.1 属于"容积超额"，**等重量方案定案再摆**（≈ 400 000 mm³，占七成以上）

| 典型对 | 体积 | 说明 |
|---|---|---|
| `servo__trunk_pitch` ↔ `Raspberry Pi 4B` | 27 683 | 躯干内净空不够 |
| `Raspberry Pi 4B` ↔ `3S 电池` | 14 450 | 两件抢同一层 |
| `electronics_deck` ↔ `Raspberry Pi 4B` | 13 199 | 托盘标高与实际件高不符 |
| `servo__trunk_roll` ↔ `电池` | 10 678 | 同上 |
| `battery_tray` ↔ `Raspberry Pi 4B` | 10 303 | 同上 |
| `head_shell` ↔ `servo__head_pitch` | 6 516 | 头壳内腔容不下舵机 |
| `torso_frame` ↔ `XL4015` | 7 214 | 背挂模块与框架 |

> 这一组**不建议现在动**：躯干件高、托盘标高、背挂位置都由"结构 500 g"的预算假设推导，
> 重量方案一改（减重 / 换舵机 / 减 DOF），这些位置全部要重排。

### 4.2 属于"关节布局"，可以在布局定案时一并解决（≈ 145 000 mm³）

| 典型对 | 体积 | 说明 |
|---|---|---|
| `shoulder_pitch` ↔ `shoulder_roll` 舵机 | 16 255 ×2 | 两轴相交，机体互相咬；需把其中一个机体沿**自身轴向**挪开 |
| `pelvis_frame` ↔ `hip_yaw` 舵机 | 12 533 ×2 | 框架的舵机座按旧包络（25.2 厚）开的口 |
| `torso_frame` ↔ `shoulder_roll/pitch` 舵机 | 11 659 / 10 081 | 同上 |
| `right_hip_roll` ↔ `right_gripper` 舵机 | 8 111 | 髋 roll 机体伸进了手臂空间 |
| `pelvis_frame` ↔ `trunk_roll` 舵机 | 8 327 | 同上 |
| `hip_yaw` ↔ `hip_roll` 舵机 | 7 521 ×2 | 19.6 mm 关节距放不下两个 35 mm 机体 |
| `foot_plate` ↔ `ankle_pitch` 舵机 | 4 658 | 足板让位不足 |

> 这组是**真问题**，但解法都是"把某个件沿某方向挪 X mm"或"把框架的座按新包络重开"，
> 属于布局决策：**每挪一次都会改变整机包络与质量分布** →
> 与重量方案耦合，建议合并成一件事做，不要零敲碎打。

### 4.3 已排除的一种"看起来能修"的方案（有实验数据）

**只把舵机绕自身轴转 180°（4 孔阵 4 重对称，不影响拧螺钉）解决不了这组干涉。**
`design/cad/fit_clocking.py` 对 22 个关节逐个试了 `parent` 与 `-parent` 两个钟点：

| 关节 | 翻转前 | 翻转后 | 结论 |
|---|---|---|---|
| `head_pitch` | 3 450 | **2 011** | 有效但没清零 |
| `left_hip_pitch` | 3 978 | 3 913 | 基本无效 |
| `left/right_hip_roll` | 7 521 | 7 521 | **完全无效** |
| `left/right_shoulder_roll` | 16 255 | 16 255 | **完全无效** |
| `right_gripper` | 8 223 | 8 223 | **完全无效** |

原因：这些互咬发生在**沿轴方向**（两个机体在轴向上占据了同一段空间），
绕轴转 180° 不会改变轴向占据 → 必须**把机体沿自身轴向挪开**，
或改框架上的舵机座位置。两者都会动到包络与质量 → 属布局决策。

### 4.4 修正带来的一个副作用（质量变了，做重量方案时用新数）

零件包络按真实舵机尺寸修正后，"把 35 当垂直高度"造成的虚胖消失：

| 项 | 修正前 | 修正后 |
|---|---|---|
| 打印结构件合计 | 1 789 g | **1 404 g**（−22%） |
| 整机合计 | 3 435 g | **3 050 g** |
| 踝关节力矩需求 | 1.68 N·m（114% / 占堵转×50%） | **1.49 N·m（102%）** |
| 整机包络 | 129 × 223 × 418 mm | **129 × 204 × 407 mm** |

> 也就是说：**讨论重量方案时请以 1 404 g 结构 / 3 050 g 整机为基线**，
> 旧表格里的 1 789 / 3 435 是"舵机尺寸用反"时期的虚高值。

---

## 5. ⚠️ 纪律提醒（给下一个会话）

1. `orient()` 已改为显式矩阵，**不要再退回 `SetTransformation(gp_Ax3, gp_Ax3)`**。
2. 任何"舵机的 35 / 24.7 / 轴心位置"取值，一律从 `kit.servo_frame()` 拿，
   不要在零件里再写一遍常量。
3. 改完零件/装配后**必须**跑 `audit_assembly.py`：只看图容易漏，数字不会骗人。
4. 本记录只做装配一致性；**形体细化（减重窗拓扑、加强筋、圆角）等质量路线定案后再做**。

---

## 6. 追加诊断：重叠量级的"性质"与一条硬根因

> 触发：复查时提出质疑——"这些重叠体积完全不合理，这可是零件设计模型而不是包络轮廓"。
> 质疑成立。下面是把它量化后的结论，以及**订正我在第 4.1 节的错误归类**。

### 6.1 判读指标：看**重合率**，不要看绝对体积

`audit_assembly.py` 已升级：每对干涉同时给出
**重合率 = 交集体积 ÷ 较小件自身体积**，并据此分性质：

| 判定 | 阈值 | 含义 | 处置 |
|---|---|---|---|
| ❌ **摆放错误** | 重合率 ≥30% 或 ≥5 000 mm³ | 两个零件被指派到**同一块空间** | 改尺寸**无解**，必须改摆放/基准 |
| ⚠️ 让位不足 | 5–30% 或 500–5 000 mm³ | 局部让位不够 | 改尺寸/倒角/挪位可解 |
| · 局部干涉 | 其余 | 小干涉或假阳性 | 逐条确认 |

修正后的实测（2026-09-11 晚）：

```
性质判定：摆放错误 37 对 ｜ 让位不足 152 对 ｜ 局部干涉 52 对
体积分布：≥10 000 mm³ 11 对 ｜ 5 000–10 000 12 对 ｜ 1 000–5 000 106 对
          ｜ 100–1 000 97 对 ｜ <100 15 对
按零件统计：Raspberry Pi 被侵占 90%、torso_frame 56%、trunk_pitch 舵机 127%、
          battery_tray 147%、electronics_deck 174%（自身体积占比）
```

**"某零件被侵占 > 自身体积"在物理上不可能**——只有当一个零件同时插进好几个邻居时才出现。
这直接说明：**问题不在配合公差，而在摆放层**。

### 6.2 硬根因（已复现）：电子件摆放分支的 key 不匹配，7/7 全部落空

`assembly.py` 里有两张"落座/背挂"表：

```python
ELEC_DECK_TOP = {"compute": 42.0 + 2.6, "battery": 18.0 + 2.6}
ELEC_BACKPACK = {"mcu": 0, "servo_driver": 1, "bec": 2, "amp": 3, "speaker": 4}
```

而 `placements.json` 里的 id 是**长显示名**：`Raspberry Pi 4B (4GB)`、`STM32F405RGT6 核心板`、
`飞特 URT-1 总线驱动板`、`XL4015 降压模块`、`PAM8403 功放模块`、`4Ω 3W 腔体喇叭 Φ28`、
`3S 11.1V 2000mAh 10C 锂聚合物 (XT60)`。

复现：

```bash
.venv-cad/bin/python -c "
import sys, json; sys.path.insert(0,'design/cad'); import assembly as A
pl = json.loads((A.DESIGN/'placements.json').read_text(encoding='utf-8'))
ids=[e['id'] for e in pl['electronics'] if e['link']=='torso_upper']
print('命中托盘分支', [i for i in ids if i in A.ELEC_DECK_TOP])
print('命中背挂分支', [i for i in ids if i in A.ELEC_BACKPACK])"
# → 两个分支都是 0 件（共 7 件）
```

**后果**：7 件电子件全部停在 `placements.json` 的原始局部坐标上
（树莓派 local z=−12、电池 local z=−26），而躯干舵机就占着那一段
→ 树莓派与 `trunk_pitch` 舵机 69% 重合、与电池 25% 重合、与托盘 71% 重合……
**这是静默失效（silent no-op），不是设计冲突。**

> **订正**：本记录第 4.1 节把这一组归为"容积超额，等重量方案定案再摆"——**归类错了**。
> 其中相当一部分是**代码 bug**（分支从未生效），跟重量方案无关，应当**现在就修**。

### 6.3 这一类问题缺的不是"数值"，是"判据与契约"

连续两次踩同一类坑（舵机坐标系、电子件分支），共同点是
**摆放层没有"基准 + 配合 + 断言"**：位置靠手写常数、坐标系隐式转换、
分支 key 靠字符串匹配且失败不报错。

因此需要补齐的能力（分成三类，前两类适合向外要世界知识，第三类是本仓库工程）：

| 类别 | 缺什么 | 怎么补 |
|---|---|---|
| **判据（数值表）** | 界面类别 → 允许重叠/最小间隙 → 判读阈值 → 假阳性清单（贴合面、相切、螺纹、标准件、阵列件） | **向 Gemini 要**：`第4轮-给Gemini的提示词-装配校核方法与堆叠惯例.md` K1/K3 |
| **方法（惯例）** | 基准/配合（datum/mate）建模惯例；DFA 可达性与顺序校核；极限姿态扫掠体积校核；小轴距多舵机堆叠惯例；打印框架座口与减重规则；该结构族失效模式清单 | **向 Gemini 要**：同一份 prompt 的 K2/K4/K5/K6/K7/K8 |
| **工程（本仓库）** | ① 稳定 ID（`placements.json` 的 `id` 只作显示名，另加 `kind`/`ref` 字段做程序索引）；② **分支未命中即失败**的断言；③ 零件"基准声明"（datum）与装配断言；④ 体检进入 CI（阈值：摆放错误=0、关节轴对齐=22/22、每零件单实体） | 我实现（等 K1/K2 的回值确定阈值与字段设计） |

### 6.4 立即可做的两件事（不依赖外部输入）

1. **修 6.2 的静默失效**：给 `placements.json` 的电子件加稳定 `kind` 字段，
   装配层改用 `kind` 索引；命中数为 0 时**直接抛错**而不是静默跳过。
2. **给摆放加断言**：每个零件装配后立刻检查"是否与已放置件发生 ≥5 000 mm³ 的重合"，
   一旦发生就报出零件名并中止——把"摆错"从"事后体检"提前到"装配时立即失败"。

---

## 7. 第二轮：把"摆放层"整条链的静默失效修掉

> 执行：2026-09-11 晚（与 Gemini 第 4 轮并行）。范围同上：只修一致性，不做形体细化。

### 7.1 这一轮修的 5 条（都是"约定不一致"，不是"设计取舍"）

| # | 问题 | 性质 | 修法 |
|---|---|---|---|
| 1 | **电子件摆位规则整批静默失效**：`components.json` 只存显示名 → `gen_placements.py` 把显示名写成 `id` → 装配层用短 key（`compute`/`battery`…）匹配 → **7/7 件全部落空**，全停在原始坐标（正好压在 `trunk_pitch` 舵机上） | 数据/代码约定的静默失配 | 数据层加**稳定 `kind`**（`components.json` 10 条 + 生成器输出 `kind`）；装配层按 `kind` 解析、**解析不到直接报错**；另设 `ELEC_RAW_OK` 白名单——想"就用原始坐标"必须显式签字 |
| 2 | **占位体"底面在原点"、落座公式按"中心"** → 每件高半个身位，电池顶穿电控托盘（11 937 mm³） | 几何约定不一致 | 占位体统一为**几何中心为基准**；`components.json` 里 3 件历史数据（相机/拾音/IMU）按中心约定补半身位，实物位置保持不变 |
| 3 | **两层地板只算一层**：框架层板（上表面 20.6/44.6）+ 插入件自己的 3 mm 底板 → 真正承放面在 23.6/47.6 | 尺寸链漏项 | 新增 `TORSO_BAY_LOAD_Z` / `TORSO_DECK_LOAD_Z`，落座标高引用"承放面" |
| 4 | **同一套 z 写两遍**：`torso_frame` 内建层板 18/42，而 `battery_tray`/`electronics_deck` 建在 z≈0，装配层又按 link 系原点摆 → **两个打印件自己先撞上**（9 942 mm³，重合率 53%） | 缺单一真值来源 | 层高提升为 `skeleton.TORSO_*` 常量：框架层板、插入件标高、装配落座标高**三处同源** |
| 5 | 舵机机体互咬（肩 pitch↔roll 15.5 k×2、髋 yaw↔roll 7.2 k×2…） | 可用"绕自身轴 180° 翻转"缓解 | `fit_clocking.py` 做**全局翻转优化** → 采纳 7 处翻转，舵机间干涉 **67 325 → 55 691 mm³**；翻转只作用于舵机自身，笼/叉不动（安装孔阵 4 重对称，座口仍对得上） |

另落地两项工程设施：

- **共用判定原语** `design/cad/fitcheck.py`：干涉体积/最小距离/包围盒重叠/性质判定**只有一份实现**，
  `assembly.py`（装配时校验）· `audit_assembly.py`（体检）· `fit_clocking.py`（求解）共用，
  杜绝"体检说没问题、装配时放行"。
- **构建闸门**：`assembly.py` 装配后立即校验并列出『摆放错误』（`--strict` → 退出码 1）；
  `build.sh` 新增步骤 2.5 跑体检，`STRICT=1` 即失败，`SKIP_AUDIT=1` 才允许带病继续。

### 7.2 数据（累计）

| 指标 | 起点 | 第一轮后 | 第二轮后 |
|---|---|---|---|
| 相交体积合计 | 741 740 mm³ | 545 256 | **415 323**（累计 −44%） |
| 干涉件对 | 320 | 241 | **215** |
| 『摆放错误』对 | —（当时按体积看，37） | 37 | **26** |
| 舵机轴向 ↔ 关节轴 | yaw 类整 90° 错 | 22/22 | **22/22** |
| 零件内"飘块" | `joint_cage` 4 实体 | 15/15 单实体 | 15/15 单实体 |

### 7.3 这一轮**暴露出来、需要拍板**的两项

| 项 | 现象 | 可选处置 |
|---|---|---|
| **背挂电子件** | "背挂"分支修好后真的生效了：STM32 / URT-1 / XL4015 / 功放 / 喇叭 被挂到躯干背面 X=−60 → 整机深度 **123 → 198 mm**；其中 3 件（STM32 / 功放 / 喇叭）**与任何零件都不接触（悬空）**，缺背挂支架 | ① 补一个背挂支架零件（接受深度 198）；② 改回舱内并给躯干扩容；③ 减件（换更小模块） |
| **躯干 / 头部容积** | 树莓派落到电控层承放面（47.6）后顶部到 64.6，与 `head_yaw` 舵机笼相撞（6 316 mm³）；头部相机与 `head_pitch` 舵机 8 041 mm³ | 与"结构 500 g vs 实建 1 404 g"的重量方案**同一批**决策：扩容 / 减件 / 换件 |

**剩余 26 对『摆放错误』已全部落在上表两类**（布局与容积），
不再有"代码约定不一致"造成的假干涉——这是本轮与上一轮的本质区别。

### 7.4 纪律（已同步写进代码注释）

1. 程序一律按 **`kind`** 索引配件；`id`/`name` 只给人看；**解析不到就报错**，不许静默跳过。
2. `placements.json` 的 `position_mm` 按**几何中心**；落座标高一律引用 `skeleton.TORSO_*` 常量。
3. 零件局部坐标系与 link 系的对应关系必须在零件里声明（基准），不在装配层用魔法数补偿。
4. 改完必须过 `build.sh`（含体检）；要提交时用 `STRICT=1`。

---

## 8. 第 5 轮：髋/肩簇错轴（与 Gemini 并行）

> 执行：2026-09-11 夜。判定见 `第5轮-本仓库执行提示词-髋肩簇错轴重做.md`：
> 19.6 mm 轴距塞两只 35 mm 厚机体，钟点翻转消不掉——**只推倒髋/肩连接件**。

### 8.1 布局实验（`fit_stagger.py`，只动舵机占位）

沿自身输出轴扫描 ±40 mm / 5 mm。贪心一轮后 **6/6 对舵机-舵机干涉 → 0**。

| 关节 | stagger (mm) | 来源 |
|---|---|---|
| `hip_yaw` L/R | 0 | 不动 |
| `hip_roll` L/R | **+35** | 沿自身 shaft |
| `hip_pitch` L/R | **+30** | 沿自身 shaft |
| `shoulder_pitch` L/R | **+35** | 沿自身 shaft |
| `shoulder_roll` L/R | 0 | 不动 |

这些数写入 `JOINT_SCHEME[*]["stagger"]`，禁止在装配层另写一套。

### 8.2 新零件族（旧 `compact_adapter` 只留给颈/腰 27.5 mm）

| 件 | 件数 | 职责 |
|---|---|---|
| `cluster_horn_arm` | 6 | 锁上一级金属舵盘（I2），C 形外侧臂把下一级轴线拉到 19.6 mm |
| `cluster_outrigger` | 8 | 在轴距外侧抱住错开后的机体两端面 4×M2.5，副轴侧 MF106ZZ |
| `backpack_plate` | 1 | 选项 A：5 件背挂电子件的公共座 |

### 8.3 数据（相对第 4 轮第二轮后）

| 指标 | 第 4 轮后 | 第 5 轮后 |
|---|---|---|
| 相交体积合计 | 415 323 mm³ | **278 335**（再 −33%） |
| 『摆放错误』对 | 26 | **21** |
| 髋/肩舵机-舵机『摆放错误』 | 肩 15.5 k×2、髋 yaw↔roll 7.2 k×2 | **0** |
| 关节轴对齐 | 22/22 | **22/22** |
| 装配件数 | 80 | 87（失败 0） |
| 整机包络 | 129 × 204 × 407 | **198 × 263 × 407**（背挂 + 肩簇外甩） |
| 结构件质量 | 1404 g | **1490 g**（新件 +86 g；外购路线仍是减重正道） |

相机沿 +X 再送出 20 mm：与 `head_pitch` 从 8 041 mm³ / 摆放错误 → 3 556 mm³ / 让位不足。

背挂 5 件 + `backpack_plate` 自成一个连通分量（不再悬空）。深度 198 mm ≤ 赛题 300 mm，本轮接受。

### 8.4 本轮**没有**清掉、留给重量方案 / 框架重开的

- 骨盆座口仍按旧 35 mm 包络开，`hip_yaw` / `trunk_roll` 机体还在穿 `pelvis_frame`
- `torso_frame` 与 `shoulder_roll`、树莓派、`head_yaw` 笼
- 肘叉 × 夹爪舵机（膝/踝/肘族，本轮明确不做）
- 树莓派顶部仍撞 `head_yaw` 笼（扩容/减件，与重量方案一批）

### 8.5 纪律（新加）

髋/肩舵机的 `stagger` 必须来自 `fit_stagger.py` 的表。新件几何从 `kit.servo_frame()` / `IF` 取，禁止再写 35 / 24.7 / 10.2。

### 8.6 Gemini S1–S6 核验后的吸收（2026-09-12）

原文 `第5轮-Gemini回复-无耳舵机错轴支架.md`，逐条 `第5轮-Gemini回复核验记录.md`。
拓扑与本轮 CAD **独立同源**（并行，不是按它的数改的）。

| 采纳 | 不采纳 |
|---|---|
| S1-B / S2：不对称 C 臂 + 副轴桥架；禁止对称 U、嵌套万向节、舍弃副轴 | 端面孔是「盲孔」、咬合 3–4 mm（实测通孔深 2.1/2.5） |
| S3：先锁上一级舵盘 → 滑入第二级 → 最后合副轴 | 机体错开 8–12 mm 就够（`fit_stagger` 收敛在 30–35 mm） |
| S4：8–12 件按 PCD14 出激光/CNC，不改通用耳孔架；承力臂 intended 6061-T6 | 通孔 Φ2.6（仓库 M2.5 = 2.7，全库 +0.2 约定） |
| S5：19.6 仍大于径向下限 17.5 量级 → **不改 URDF** | 同轴放宽到 0.08（仓库 0.05）；过孔从 8.0 降到 5.5 |
| S6 阈值进 `ASSEMBLY.cluster_fit`，全部 `provisional` | 「线座在长端 35 mm 侧」——官方图纸未标，`cable_exit=side` |

代码侧只做了两件不覆盖标定的事：`cluster_outrigger` 补 MF106ZZ 法兰沉台；`cluster_horn_arm` 标 intended 6061，质量仍按 PETG。
