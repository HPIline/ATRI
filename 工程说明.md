# A.T.R.I. 工程说明（多机协作版）

本说明面向 Windows + RTX 4060 与 macOS（Apple Silicon）两台机器的协作开发。
核心原则：**改动前先 `git pull`，改动后分小步 `git commit && git push`，禁止 `git push --force`。**

## 1. 项目是什么

A.T.R.I.（Autonomous Tabletop Robotic Intelligence）桌面自主人形智能项目。
软件采用“大脑—小脑”双层架构，纯 Python 标准库实现，无硬件、无 VLA 依赖，
在任意平台可直接编译、测试、跑通五项赛题任务卡演示。

```
大脑 Brain      JSON 任务卡 + FSM + 五项技能（face/qr/carry/kick/dance）
小脑 Cerebellum 22 DOF 步态生成 + 踢球/抓取/舞蹈动作 + 脚本运动调度
感知 Perception 人脸 / 二维码 / 球检测（Mock + OpenCV）
语音 Voice      离线关键词识别 + TTS（Mock / macOS say）
仿真 Sim         Mock 感知 + Mock 舵机，全流程闭环演示
```

## 2. 仓库地址与克隆

```bash
git clone https://github.com/HPIline/ATRI.git
cd ATRI
```

> 默认分支：`main`。不要在 `main` 上直接互相覆盖，功能开发建议开分支：
> `git checkout -b windows-sim`，合并前先 `git pull --rebase origin main`。

## 3. macOS（本机 M4）环境

- 系统自带 Python 3.9.6 即可跑核心代码，无需安装任何第三方包。
- 可选：`/usr/bin/python3` 与 Homebrew Python 均可，版本 ≥ 3.9。
- macOS TTS 可用 `atri.voice.MacOSTTS` 调用系统 `say`。

```bash
cd ATRI
python3 -m compileall -q atri run_demo.py && echo "compile OK"
python3 -m unittest discover -s tests -v
python3 run_demo.py
```

## 4. Windows + RTX 4060 环境（仿真主力）

### 4.1 需要安装

| 软件 | 版本建议 | 用途 |
|---|---|---|
| Git for Windows | 最新 | 拉取/推送代码 |
| Python | 3.10 或 3.11 x64 | 运行控制器与测试 |
| Webots | R2023b 或更新 | 机器人仿真（首选） |
| 可选 PyBullet | `pip install pybullet` | 备选仿真 |
| 可选 OpenCV | `pip install opencv-python` | 真摄像头视觉联调 |

安装时勾选 “Add Python to PATH”。验证：

```powershell
python --version
git --version
```

### 4.2 跑通核心代码

```powershell
cd ATRI
python -m compileall -q atri run_demo.py
python -m unittest discover -s tests -v
python run_demo.py
```

看到 `闭环演示完成: 5/5 项任务通过` 即环境正常。

### 4.3 Webots 控制器

1. 打开 Webots，用内置人形机器人（如 Nao/DARwin-OP）新建世界；
2. 在机器人节点 `controller` 字段选择 `<extern>`，或在 Webots 中指向
   `webots/controllers/atri_controller/atri_controller.py`；
3. 对照 `webots/controllers/atri_controller/joint_mapping.json` 检查/修改电机名映射；
4. 运行仿真，控制器会调用 `atri` 包执行任务卡。
5. Webots 标定动作后，按 `软件/atri/action_library/README.md` 导出关键帧 JSON。

> Webots 的 Python 版本要与安装 Webots 时绑定/选用的 Python 一致；
> 控制器代码会自动把仓库根目录加入 `sys.path`，因此无需安装 `atri` 包。

## 5. 目录结构

```
ATRI/
├── 工程说明.md                  # 本文件
├── ATRI_Project_Presentation-定稿.pptx
├── ATRI_Project_Presentation.pptx
├── 附件3：学生版系统操作手册.docx
├── 项目文档/                    # 项目综述、技术方案、BOM/指标、答辩Q&A、修改意见
├── 材料汇总/deepseek-harness/    # 历史材料汇总
├── .github/workflows/ci.yml     # GitHub Actions CI
├── 软件/atri/                   # 软件主工程
│   ├── atri/                    # 核心包（brain/cerebellum/perception/voice/qrgen/action_library）
│   ├── config/                  # 机器人配置
│   ├── task_cards/              # 五项赛题任务卡
│   ├── action_library/          # 动作库 JSON 规范 + 示例
│   ├── tests/                   # 单元测试
│   ├── run_demo.py
│   └── README.md
└── webots/                      # Webots 控制器（Windows 仿真）
    └── controllers/atri_controller/
```

## 6. 协作规范（重要）

1. **改前先拉**：`git pull --rebase origin main`
2. **小步提交**：一个功能一个 commit，commit message 用中文或英文均可，写清楚改了什么
3. **禁止 force push**：`git push --force` 会覆盖别人代码，一律禁止
4. **冲突处理**：遇到冲突在本地解决，不要直接覆盖对方文件；不确定就截图到群里问
5. **大文件**：单个文件超过 50MB 不要直接提交，先放到网盘/群里，仓库里放链接或说明
6. **不要提交**：`__pycache__/`、`.venv/`、`.DS_Store`、视频缓存、Webots 自动生成的临时文件

## 7. 常见问题

- **Windows 上 `python -m unittest` 报找不到 `atri`**：确认在仓库根目录 `ATRI/` 下执行，且不要进入 `软件/atri` 后再切到 `atri/atri`。
- **Webots 控制器 import 失败**：检查 Webots 的 Python 版本，以及是否在 Webots 中设置了正确的 controller 路径。
- **端口/权限问题**：Windows 首次运行 Webots 如被防火墙拦截，选“允许”。
- **换行符**：Windows 与 Mac 混用时统一 UTF-8；Git 可设置 `git config --global core.autocrlf input`（Mac）/ `true`（Windows）。
- **CI 不跑**：如果 GitHub Actions 没有出现在 PR/push 上，检查 GitHub token 是否勾选 `workflow` 权限。

## 8. 当前状态

- [x] 材料归集
- [x] 软件骨架：任务卡、FSM、22 DOF 配置、脚本运动控制
- [x] 核心代码零第三方依赖，编译/测试/演示通过（51 项测试）
- [x] VLA 已弃用，运动控制为脚本动作库 + 步态生成
- [x] 视觉感知接口 + Mock + OpenCV 代码（Mac 编写，Windows 真机联调）
- [x] 语音交互接口 + Mock + macOS TTS
- [x] 二维码 JSON 指令生成器
- [x] 动作库 JSON 规范 + 示例 + 校验/回放
- [x] 项目文档：技术方案、BOM/指标、答辩 Q&A
- [x] GitHub Actions CI 工作流（需 token 勾选 workflow 权限后生效）
- [ ] Webots 仿真控制器联调（Windows）
- [ ] OpenCV 真实摄像头联调（Windows/样机）
- [x] 企划书/PPT 定稿（PDF 已导出）
- [ ] 上传省赛系统
- [ ] 硬件到位后的实机标定