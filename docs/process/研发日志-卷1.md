# A.T.R.I. 研发日志

> 一台 22 自由度的桌面双足人形机器人。身高 373 mm，整机 2146 g，全机 22 只同型号总线舵机，
> 核心控制代码只用 Python 标准库——没有硬件也能跑通全部任务流程。
>
> 参赛：中国国际大学生创新大赛（2026）陕西赛区 · 人形机器人专项 · 小人形组
> 本仓库 41 次提交，跨度 2026-09-10 19:00 → 09-11 17:57。

> **⏱ 时间截面声明（2026-09-11 补记）**
>
> 本卷是**截至 2026-09-11 17:57（提交 `6d4b077`，当时为第 41 次提交）的快照**。
> 上文"41 次提交""跨度 → 09-11 17:57"说的都是这个截面，**不是仓库当前状态**
> （仓库在此之后继续推进，见 `git log`）。质量与力矩数字一律采用
> **v2 口径**（结构件 500 g、整机 2146 g、腿部 1.037 N·m ≈ 额定 106%）。
>
> 该口径此后**两度更动**：**v3** 先切到 CAD 实装（结构件 1790 g、整机 3437 g、
> `trunk_roll` 2.092 N·m / 判据 142%）；继而《装配一致性修正记录》查出舵机包络的
> **35 与 24.7 用反**（并连带修正 `orient()` 的轴向映射），重算为
> **结构件 1404 g、整机 3050 g**，踝关节 1.49 N·m、占"堵转×50%"判据 **102%（仍超）**。
>
> **本条不追平后续数字**——现行为准请看 `design/handoff/装配一致性修正记录.md` §4.4、
> `design/handoff/新架构参数总表.md` §3/§3.1 与
> `design/reference/cad_assembly_measurements.json`。
>
> 本卷**有意不回溯改写**：它记录的是"当时基于什么数字、做了什么判断"，
> 数字换代本身就是研发过程的一部分。读数字以最新记录为准，读**判断与订正的方法**看本卷。

> **⏱ 时间截面声明（2026-09-12 03:00 补记，第 6 轮结束）**
>
> 截面：提交 `b0b6221`（本地，本轮多线程产物收编后）。此后仍有 3 条线程在跑
> （真机总线驱动 / 过期口径 Top-10 修正 / CAD 让位修正），其产物未计入本节。
>
> **当前实测口径**（全部来自本轮命令输出，见 §三 与 §六）：
>
> | 项 | 现行值 | 与上一截面（1404 g / 3050 g）的关系 |
> |---|---|---|
> | 整机包络（实建） | **146 × 263 × 407 mm** | 深度由 198 → 146（背板链路再变），Y=263 由肩部外挂件决定 |
> | 结构件质量（实算） | **1490 g / 81 件** | +86 g（多了错轴簇新件） |
> | 整机质量 | **≈3136 g（纸面推算，重量方案未定案）** | +86 g，仍**未定案** |
> | 干涉总量 | **193 677 mm³ / 203 对** | 266 134 → 193 677（本轮 −27%） |
> | ❌ 摆放错误 | **13 对** | 19 → 13 |
> | 关节轴对齐 | **22/22** | 不变 |
> | 自动化测试 | **186 项全绿** | 169 → 186 |

> **⏱ 再更新（2026-09-12 晚，队友分支 `atri-next` 第 6–10 轮合入后，`main` @ `ecf47b3`）**
>
> 权威口径第三次换代：整机包络 **160 × 268 × 407 mm**、机械零位**摆放错误 0 对**
> （`pair_inspect --errors` 为空）、干涉总量 **160 423 mm³**、关节轴对齐 22/22、
> 布局门禁 `test_layout.py` **15 项**通过；结构件 **1390.4 g / 81 件**（对比合入前 1490 g，**−7%**）、
> 整机 **≈3036 g**（纸面推算，重量方案仍未定案）。目录改名 `软件/` → `software/`，
> 文档迁入 `docs/`（`docs/process`、`docs/research/项目文档`、`docs/contest`）。
> 上表里的 146×263×407 / 193 677 mm³ / 13 对 / 1490 g 是**合入前**的值，保留作演进记录。
>
> **同时查出两件对参赛材料有直接影响的事**：
> ① `kit.printed_mass()` 的薄壁近似在 3 mm 壁件上退化成**实心质量**（壳体积 ≥ 总体积），
>    ⇒ 1390 g 应读作**上限**，厚实区的填充折扣没体现，需切片器标定一件校核；
> ② **现有 Webots 世界是零重力的**（`webots/worlds/atri_22dof.wbt` 里 `gravity 0`、无地面、
>    无 `maxTorque`、无扭矩反馈）——现有的"5/5 任务"演示是**无重力运动学**，静立扭矩≈0，
>    不能当作"仿真验证过站立/行走"的证据。详见 `design/handoff/仿真请求-Webots验证清单.md`。
>
> **本轮新增的方法论工具**（这是本轮真正的产出，比数字重要）：
> `fitcheck.py`（判定原语唯一实现）、`audit_assembly.py`（整机体检）、
> `fit_clocking.py`（钟点求解，7 处翻转把舵机-舵机干涉 67 325 → 55 691 mm³）、
> **`pair_inspect.py`（把干涉体反变换回零件自己的坐标系，直接给出切口盒）**、
> `sweep_check.py`（姿态扫掠）、`tool_access.py`（螺钉可达性）、`reference_fits.py`（参考机反查）。
>
> **并行施工的规矩**（本轮立）：一个提交者、文件所有权互斥、每条线程写
> `design/handoff/线程报告-<线程名>.md` 作为跨线程通信，重 CAD 活一次只开一条线。

> **⏱ 时间截面声明（2026-09-12 补记，仿真迭代第 1–3 轮后）**
>
> 截面：提交 `522cb85`（已推送 `origin/main`）。上两条声明的数字没有错，但**已不是现行口径**；
> 现行值如下，全部来自本轮实测（**零件级** 86 件装配 + 同一套配合判据）。
>
> | 项 | 现行值 | 与上一条声明的关系 |
> |---|---|---|
> | 零位干涉（零件级） | **187 对 / 155 083 mm³** | 本轮修落位：189 对 / 159 743 → 187 / 155 083（−4 660，−2.9%）|
> | ❌ 摆放错误 | **0 对** | 不变（`pair_inspect --errors` 为空）|
> | `test_layout.py` 门禁 | **17/17 OK** | 上条写 15 项；门禁在本轮之前已增至 17 项（`2567843` 后未再改）|
> | 关节轴对齐 | **22/22** | 不变 |
> | 整机包络 | **160 × 268 × 407 mm** | 不变（本次修落位不动包络）|
> | 结构件 / 整机质量 | **1390.4 g / 81 件、≈3036 g** | 不变 |
> | Webots 离线测试 | **41 项 OK**（原 40） | 新增"每个关节阻尼必须 > 0"一条 |
> | 连通分量 | **2**（背挂簇 3.00 mm 脱开）| 仍未修 |
>
> **引用口径提醒（两条）**：① 干涉数字必须带层级——上表是**零件级**（CAD 真实占位几何）；
> 上一条声明的 160 423 mm³ 与第 6 轮截面的 193 677 mm³ 是**不同来源的口径**（前者标"合入前"），
> 与本轮的 155 083 mm³ **不能直接相减**；第 2 轮已实测 link 级体积高估约 10 倍，做决策只看零件级。
> ② `❌ 摆放错误 = 0 对` **不等于"没有穿模"**：按 `audit_assembly.py` 本届实测，187 对里是
> ⚠️ 让位不足 **114 对** + 局部干涉 **73 对**，**全部是真干涉**。
> （⚠️ 本条初稿曾写作"187 对里绝大多数是同关节配合面"——**该说法已被证伪**：
> 队友 `1b08a73` 实测 `fitcheck.is_joint_mate()` 在 189 对里只判出 **5 对**配合面，
> 184 对 `mate=False`；且该函数对 `*__limb_tube` 这类名字**数学上恒为 False**——
> "50" 是**零件数**、不是**干涉对数**，初稿把两个口径混在一句话里。已按实测改正。）
>
> **本轮的产出其实是"证伪"**（按 §五 的传统，这算又翻了一次案：翻掉的是**已经写进文档的
> 下一步动作**，不是历史数字）：
>
> 1. **"重生成 `placements.json`"是退步**（暂停文档队列第 ① 项的前提不成立）：实测会把 IMU
>    搬回 `trunk_roll` 舵机体内（900 mm³、重合率 **100%** 的摆放错误，0 ❌ → 1 ❌）。
>    派生文件里的手调值是对的，源文件才是错的 —— 本轮改为**修源文件** `components.json`
>    （IMU 归后桥座、相机对齐头壳自带的相机口袋）。
> 2. **收紧关节限位无效**：按扫掠结果把限位收一档，离线筛查给出事件 632 → 222 很诱人，
>    但**真跑一遍（限位改后重新采样）事件反涨到 719** —— 采样器本来就在限位盒内取样，
>    碰撞区落在收紧后的盒子**内部**，被切掉的那部分空间里根本没有事件。这条路不要再走。
> 3. **Webots 接触点探针此路不通**：`getContactPoints()` 只在 `Supervisor` 上（用 `Robot()`
>    第一行就 `AttributeError`）；打开 `selfCollision` 后求解器卡死在第一个 `robot.step()`
>    —— 因为每个 link 只有一个盒子 `boundingObject`，零位就大面积互相穿透（253 对里 163 对相交）。
>    要用接触点做动态验证，前提是碰撞几何换成零件级网格，那是另一个工程量。
> 4. **顺带修掉一个"看起来像控制器坏了"的真缺陷**：世界文件 22 个关节**一个阻尼都没写**，
>    零重力 + 22 个轻质连杆在启动 4 个物理步（128 ms）内把若干关节弹到上千度后卡死，
>    "有行程关节"从 18/22 掉到 11/22。生成器现已统一写死 `dampingConstant 0.5`。
>
> **证据**：新增 `docs/process/sim/仿真迭代-第{1,2,3}轮-20260912.md`（本轮三份完整日志，含原始数字
> 与对照实验）；仓库外辅助件 `_sync/cad.py`（退出码包装器）、`_sync/wbprobe/`（探针世界）。
>
> **仍未修（交设计侧）**：肘部簇三对 ❌（`前臂管↔肘舵机` 展示姿态 4 209 mm³、
> `肩俯↔肘`（极端姿态）10 080、`肘↔夹爪` 6 369）；相机占位体比 Φ37 镜头孔大 1 mm（654 mm³ 四角）；
> 背挂连通分量 2；源/派生 `size_mm` 语义混用（实物尺寸 vs CAD 占位，需拆字段）。

---

## 一、开工第一件事：立规矩

不是画图，是定几条不许破的规矩，写进 `AGENTS.md` 全流程生效：

1. **每个参数必须有出处。** 找不到出处的标 `provisional`，建模时告警。
2. **`unknown` 必须拦截构建。** 宁可构建失败，也不要用臆测尺寸出零件。
3. **能核对的一定核对。** 外部给的参数要过校验器；签不动就标"待确认"，不编数。
4. **别把"我没取到"说成"这东西没有"。** 超时要复测再下结论。
5. **登录墙、验证码、账号类操作走人工。** 不写脚本绕。

第 4 条是踩了坑之后补的，第五节第 2 案。

---

## 二、赛题与我们的答案

| 小人形组要求 | A.T.R.I. | 裕度 |
|---|---|---|
| 高度 ≤600 mm | 373 mm | 227 |
| 宽度 ≤300 mm | 190 mm | 110 |
| 厚度 ≤300 mm | 123 mm | 177 |
| 电压 ≥7.4 V | 11.1 V（3S） | 3.7 V |
| 关节 ≥18 | **22** | 4 |
| 每条腿 ≥4 | 5（髋 3 + 膝 1 + 踝 1） | 1 |
| 上肢躯干 ≥10 | **12**（头 2 + 臂 8 + 躯干 2） | 2 |
| 传感器：IMU/摄像头/麦克风/扬声器 | 齐全 | — |

**那 2 个躯干关节（`trunk_roll` / `trunk_pitch`）就是全部裕度。**
删掉它们，含头部口径下变成 10 恰好卡线、不含头部口径下掉到 8 直接不合格。
所以这两个关节在我们的设计里是**不可协商项**，不是可优化项。

一个反直觉的对照：同类商用开发套件（幻尔 TonyPi 18 DOF / TonyPi Pro 20 DOF / Tonybot 17 DOF）
**躯干自由度全部为 0**，按同一口径分别是 8 / 10 / 7。这不是说商用机不行——
它们是通用开发套件，赛题是专项赛。但**"必须有躯干自由度"这条确实是本赛题特有的约束**。

> ⚠️ "上肢躯干"是否含头部，赛题原文未定义。我们采用**含**（人形机器人不一定有头，
> "头部"若不计入该项就没有意义）。若评委采用不含口径，我们掉到 10，仍达标但零裕度。
> 已列入待确认清单。

---

## 三、时间线（按真实提交）

| 阶段 | 干了什么 | 留下的坑与教训 |
|---|---|---|
| 09-10 19:00–20:05 | 建库、加 CI | CI 工作流加了两遍——第一次 token 缺 workflow 权限，移除后重新加回 |
| 09-10 22:33–23:34 | 软件栈（任务卡 / FSM / 技能库）+ Webots 联调 | R2025a 里 **5/5 任务、22/22 关节绑定**跑通；顺手修了个坑：生成脚本用了 `Path.write_text(newline=)`，那是 **Python 3.10+ 才有的参数** |
| 09-11 00:13–01:04 | 从几何基元走向真实设计 | 用真实元件参数校验模型，**一次查出 10 处配合失败**；随后接上 OpenCASCADE 真 CAD 内核，出了关节模块、连杆、足板；同晚做了成本核算 |
| 09-11 00:53–02:07 | 答辩 PPT 代码化生成 | 51 页 PPT 由脚本生成（设计系统 + 构建器），不手搓 |
| 09-11 12:02–13:29 | 架构 v2 | 身高对齐参考机 373 mm，模型质量从"结构占位预算"改成真实分布，全部文档同步口径 |
| 09-11 17:57 起 | CAD 骨架零件库 + 整机装配 + 参数核验 | 订正舵机参数；随后拿到**飞特官方规格书**，把扭矩判据整个翻了一遍（第 2 案） |
| 09-11 深夜 | 装配一致性修正（第 3 案收尾） | 查出舵机包络 **35 与 24.7 用反**，连带修正 `orient()` 的轴向映射；干涉 741 740 → 545 256 mm³ |
| 09-12 凌晨 | 错轴拓扑重做髋/肩簇 + 校核工具链 | 不再抠现有碰撞，改拓扑从根上消共面重叠；确立"判定原语只留一份实现"（`fitcheck.py`），体检数字开始可复现 |
| 09-12 凌晨 | **第 6 轮：多线程并行施工（6 条）** | 4 条线程跑完并落盘、1 条半成品但有效、1 条（Grok 慢路由）零产出；据此订出并行施工的规矩与"重 CAD 单线"原则（见 §五 第 4 案、§八） |

**当前规模**（2026-09-12 补记）：核心控制包 27 个模块 / 约 1.5k 行；CAD 层 6.5k 行
（新增 `fitcheck.py` / `audit_assembly.py` / `fit_clocking.py` / `pair_inspect.py` /
`sweep_check.py` / `tool_access.py` / `reference_fits.py`）；
**186 项自动化测试全绿**；URDF 23 link / 22 joint，可直接进 PyBullet 或 Webots；
整机装配 86 件全部构建成功、关节轴对齐 22/22、连通分量 1。

---

## 四、六个关键决策

**1. 为什么是 22 自由度**
赛题卡三处：总数 ≥18、每腿 ≥4、上肢躯干 ≥10。腿 5×2 + 臂 4×2 = 18 时上肢躯干只有 8，
必须补 2 个躯干关节。**22 不是"越多越好"，是刚好补上缺口**。

**2. 为什么全机一个舵机型号**
22 只全用飞特 STS3215（12V 版）。分档买"膝盖用大的、手臂用小的"这套方案算过：
**+1400 元、+350 g**，而且增重会反过来推高力矩需求，容易陷入"越换越重"的螺旋。
**代价**：躯干那个受力最大的关节裕度最紧——后来证明，这个代价比我们以为的大（见第 2 案）。

**3. 为什么身高从 475 降到 373 mm**
22 只舵机占整机 56%，**它们不随尺寸缩放**。缩尺寸只能省结构件那 500 g。
算下来缩到 373 只省约 5%（2263 → 2146 g）——**这不是"减重高招"，只是对齐参考机、
让质量账有个真实对照**。

**4. 为什么不用 20 元的 PWM 舵机**
22 只 MG996R 能省 1540 元。不行：PWM 要 22 路信号线，线束复杂度爆炸；
**没有位置回读**，做不了闭环校验和故障检测；没有温度/负载回传，做不了过载保护。
省下的钱买不回这三点。

**5. 为什么弃用 VLA，改"脚本动作库 + FSM"**
大模型直接输出关节角这条路，判断在**离线、无 GPU、30 分钟续航**的约束下不成立。
改成：任务卡（JSON）→ FSM 调度 → 预标定动作库 / 步态生成。
**全部可在 CPU 上离线跑完，每一步可复现、可测试。**

**6. 为什么不买整机**
参考机买来只能当对标，不能当作品（赛题明确"不得借用他人项目"）。
我们的选择是**自研 URDF + 仿真闭环**：模型、力矩、质量账、CAD 全在自己手里，
改一个参数全链路自动跟着变。

---

## 五、四次翻自己的案（第 4 案为 2026-09-12 补记）

这一段是这份日志最想留下的部分。**三次都是我们推翻了自己已经写进文档的结论。**

### 第 1 案：口径错了——"结构超重 300 g"是错的

**原结论**：参考机结构件约 400 g，我们 552 g，超重 300 g。
**怎么发现的**：复核时发现拿 400 g 去比根本不通——那是 **373 mm 机身**的结构质量，
而我们当时是 **475 mm** 机身。**尺寸没折算就比，是硬伤。**
**订正**：按薄壳面积折算（×1.62），参考机放大到 475 mm 约 650 g。**我们的 552 g 比它还轻 15%。**
**留下的机制**：文档里明确标注"本条为订正"，并写明初版错在哪。错误留着，不删。

---

### 第 2 案：一个扭矩判据，我们换了三次，第三次才发现前两次都偏了

这一案比较长，因为它还没结束。

**第一次（初版）**：舵机给"额定扭矩 1.0 N·m"，腿部需求 1.15 N·m → 结论 **"余量耗尽，115%"**。

**第二次**：追这个 1.0 N·m 的出处——**找不到权威来源**（仓库自己都标了"缺乏权威出处"）。
于是换判据：**堵转 × 50%**。依据是参考机的实际选型习惯反推——它 1.8 kg 整机用的舵机
堵转只有 1.67 N·m，腿关节需求约占堵转 53%。按这把尺子量：可用 1.47 N·m，需求 1.05 N·m，
**占用 71%，余量够**。当时的结论是："真正不够的是 7.4 V 版，12 V 版是够的。"

**第三次**：为了给这个判据找权威出处，去抓飞特官网。**第一次抓失败，判成"官网不可达"。**
后来复测发现——**官网是通的，只是慢，首字节要 8 秒**，我们给的超时是 20 秒。
把超时当成"站点不可达"，直接误判了自己的能力边界。给足超时后，**官方《产品规格书》PDF 到手**：

```
额定负载  10 kg·cm = 0.98 N·m @12V   （对应额定电流 900 mA，即可持续工况）
堵转扭矩  30 kg·cm = 2.94 N·m        堵转电流 2.7 A
```

**这一下把前两次都照出来了**：
- 第一次用的"额定 1.0 N·m"——**方向是对的**，官方就是 0.98 N·m。我们说它"无出处"没错，
  但结论"判据不可信"下早了。
- 第二次的"堵转 × 50% = 1.47 N·m"——**比官方额定乐观 50%**，应当作上限参考，不是设计值。
- 按官方口径重算：单腿支撑需求 **1.037 N·m ≈ 额定的 106%**。
  **而且 3S 电池末端掉到 9.9 V 时，额定还要再降额。**

**现在的状态：余量不足，这是个未解决的问题，不是已解决的问题。**
可以动的方向有三个（换更高扭矩舵机 / 降动态系数走保守步态 / 减重），
但**在买一只舵机实测连续扭矩与温升之前，任何一个方向的账都算不准**。

**留下的机制**：
1. 新增规矩——**"超时要复测再下结论"**，写进 `AGENTS.md`。
2. 官方规格书 PDF 入库（`design/cad/vendor/feetech/`），并逐页转图 + 抽文字，可复核。
3. **把"未解决"明确标成未解决。** 我们没有为了让文档好看而改回"余量够"。

> 顺带一提：官方规格书还给了 6 项反馈（负载/位置/速度/输入电压/**工作电流**/工作温度）
> 和三档电子保护（过载 >80% 堵转持续 2s、过流 >2A 持续 2s、**过热 >70℃ 切扭矩**）。
> 也就是说——**我们要实测的那条曲线，舵机自己就能回传。** 缺的只是一只舵机。

---

### 第 3 案：数据被证伪——一个"官方图纸"给的尺寸，实物上根本没有

**原结论**：`standards.py` 里写着"机身含两侧安装耳，总长 51.2 mm，耳孔距 48.5 × 10.0 mm"，
来源标注是**飞特官方 2D 工程图纸**。

**怎么发现的**：这段数据自己就不自洽。算一下：
`(51.2 − 48.5) / 2 − 1.25 = 0.10 mm` —— **孔边到外缘只剩 0.1 mm 壁厚**，注塑件不可能。

于是去找独立来源核对。从公开硬件项目（LeRobot 生态的 SO-ARM100，整机就用这颗舵机）
取到**真实 B-rep 模型**，又找到**实物包装标签照片**：

```
SIZE: A: 45.22mm  B: 24.7mm  C: 35mm     TORQUE: 30kg.cm(12V)     SPEED: 0.222sec/60degree(12V)
```

**实物机身长度就是 45.22 mm，没有 51.2 的余地；照片上也没有任何安装耳。**
后来拿到的**官方外形尺寸图**同样标 `45.23 × 24.73`（且侧视高度 35 / 36.5 与我方实测 34.7 / 36.4 吻合）。

**订正**：删除那 4 个字段。真实接口是**端面 Φ20 圆盘上的 4×M2.5 孔，方形 9.9×9.9（对角正好 Φ14）**，
两端面各一组。顺带确认舵盘节圆确实是 **Φ14**（此前只有孤证），
并修正一处速度值：**0.222 s/60°，不是 0.18**。

**连带查出一个真 bug**：CAD 的舵机占位实体里有一行
`servo_placeholder(extra_ear=True)`——**默认值就是 True**，
正在给 22 个舵机**凭空加上 44 个幽灵方块**。后果是干涉检查会报假干涉、整机包络虚增 6 mm。
已按实测几何重建，包络 51.2 → **45.2 mm**。

**留下的机制**：核验脚本 `design/cad/measure_servo.py` 入库，
**任何一条接口参数都能用一条命令重新量一遍**。原始 STEP 模型、实物标签照、官方规格书一并存档。

---

### 第 4 案：把"干涉 203 对"当成了一个数

**当时怎么想的**：整机体检给出 203 对干涉、合计 193 677 mm³，其中 ❌ 摆放错误 13 对。
直觉很自然——"把 13 对逐个切掉，指标就归零了"。

**翻案**：写了 `pair_inspect.py`，把干涉体**反变换回零件自己的建模坐标系**再看，13 对里
混着三种**性质完全不同**的东西：

| 类别 | 例子（本轮实测） | 正确的解法 |
|---|---|---|
| ① 真装配错误：零件埋在别人身体里 | `gripper_jaw` **66%** 埋在手腕舵机内 | 修**坐标系约定**：该件的建模系把喇叭口面当原点、+Z 指向舵机内部，而装配按"轴心为原点"摆放，整只爪子往壳里平移约 24 mm；它那个 Φ22 让位坑本是给输出凸台（Φ20）留的，结果切在错位置 |
| ② 零位姿态过近：几何对，是姿势不对 | 髋笼 ↔ 手腕舵机 11% | 髋在 z=−19.6/y=45、手腕在 z=−27.4/y=75，**相距仅 30 mm 而舵机宽 35 mm**——手臂自然下垂贴着骨盆。靠**默认姿势外展或关节限位**解决，**切零件解决不了** |
| ③ 电子件摆错 | USB 麦克风 **79%** 埋在头俯仰舵机里；XL4015 的 25 mm 高塞 18.4 mm 的舱 | 改摆位/换模块，属布局决策 |

**教训**：**一个聚合指标会把"设计错误""姿态几何""摆件错误"混成同一堆**。
不分类就按体积从大到小切，会先切掉承力筋——而且指标还会"变好"，等于用错误换分数。
同一天更早的一件事是同一个道理的镜像：第 5 轮我们不再抠碰撞，而是把髋/肩簇改成**错轴拓扑**，
干涉直接从 415 323 掉到 278 335 mm³——**有些"让位"问题根本不该靠让位解决。**

> **四案的共同点：错的都不是算错，是"用了一个不该信的数"。**
> 所以我们最后沉淀下来的第 1 条规矩是"每个参数必须有出处"——
> 它不是洁癖，是这四次踩坑的直接产物。
>
> 而第 2 案还多教了一件事：**"找权威出处"这件事本身也会失败。**
> 我们差一点就把"我查不到"当成了"这个数不存在"，然后拿着一个偏乐观 50% 的判据继续往下做。
> 第 4 案则补上后半句：**数对了还不够，聚合口径不对照样会把你带沟里。**

---

## 六、现在到哪了（诚实清单）

### 已完成

- 软件栈：任务卡 / FSM / 技能库 / 动作库，**169 项测试全绿**，无硬件可跑通 5/5 任务
- 仿真：Webots R2025a 实跑 5/5 任务、22/22 关节绑定，含自包含世界
- 设计层：22 DOF 运动学模型、URDF（可进 PyBullet）、质量分布、逐关节力矩需求与功率预算
- CAD 层：接入 OpenCASCADE 真内核，出可打印零件与 STEP/STL
- 对标与合规：参考机逐项核查、商用平台对照、赛题逐条自检
- 参数核验：舵机接口（安装孔阵、舵盘节圆、副轴 Φ6 伸出 4.1 mm）、官方电气规格、通信协议

### 还没做到

- ⛔ **样机尚未制造** —— 最大缺口，也是赛题要的实物展示视频的前提
- ⛔ **扭矩裕度不足（106% 官方额定）** —— 见第 2 案，**未解决**
- ⚠️ **所有性能指标都还是"未测试"**：双足连续行走 ≥1 m、人脸识别成功率、抓取成功率、
  离线续航 ≥30 min……全部停留在仿真与设计值
- ⚠️ **两项参数要实物才能闭合**：
  ① 输出轴相对机身基准面的对应关系（数字吻合，基准面未定案）
  ② 端面 Φ2.5 孔的螺纹规格（官方图纸未标；打印件对接用的是 Φ2.0 通孔）
- ⚠️ **一项赛题口径待书面确认**："上肢躯干 ≥10"是否含头部

### 下一步（按性价比排序）

1. **买 1 只 STS3215，约 90 元。** 实测连续扭矩与温升曲线，把第 2 案那个"未解决"变成数字；
   顺便闭合上面两项参数。**这比换构型、换材料、买整机都便宜。**
2. 出正式工程图（从实体投影，不再手算）；装配体极限姿态干涉检查
3. 搭样机，把"未测试"逐个变成带日期的实测数字
4. 补研发日志的后续卷次

### 2026-09-12 更新（第 6 轮结束）

**已完成（在上一清单基础上新增）**

- CAD 校核工具链：`fitcheck.py`（判定原语唯一实现）+ `audit_assembly.py`（整机体检）+
  `pair_inspect.py`（干涉下钻到局部坐标系）+ `fit_clocking.py`（钟点求解）+
  `sweep_check.py` / `tool_access.py` / `reference_fits.py`
- 真机链路软件侧：`bus_sts3215.py`（真串口驱动）+ 63 项假串口测试 + `固件/README.md` 接线标定流程
  （**全机 186 项测试全绿**；硬件到位即可上电）
- 参数口径收敛：官方额定 **0.98 N·m** 已写进全部生成器与文档；舵机接口幽灵数据（11.0 / 3.0 / 38×15）全线作废
- 装配体检可复现：86 件全构建成功、关节轴对齐 **22/22**、连通分量 1、干涉 **193 677 mm³**

**还没做到（新增两条，都是本轮暴露出来的）**

- ⛔ **电源架构被额定订正掀翻**：按官方额定 0.98 N·m 重算，舵机平均电流 6.58 → **18.24 A**，
  30 min 需标称 **11.83 Ah / 131.3 Wh（整包≈1.19 kg）**。现有 3S 2000 mAh **明显不够**，
  **电源方案必须与重量方案一起定案**（见 `design/handoff/过期口径修正记录.md`）
- ⛔ **13 对 ❌ 摆放错误尚未清零**，且已分档为"真装配错误 / 零位姿态过近 / 电子件摆错"三类（第 4 案）
- ⚠️ `atri.urdf` 有意**未回灌**新质量分布——避免把"重量方案未定案"的推算值固化进 PyBullet/Webots
- ⚠️ 踝关节 1.49 N·m（102%）是 1404 g 时期的实算，结构改 1490 g 后**待重算**

**下一步（按性价比更新）**

1. **买 1 只 STS3215 + 1 块 USB-TTL（URT-1），约 90–250 元。** 它一次能闭合两件事：
   第 2 案的扭矩/温升实测，以及真机总线驱动 12 项未验证里用单只就能验掉的 8 项
   （1 Mbps 时序、半双工 1 kΩ 接法、零位偏置**符号方向**、`verify` 开销……）
2. **定案重量与电源**（两者是同一个决策），随后回灌 URDF 并重算踝关节裕度
3. 按第 4 案的三分类清 13 对 ❌，其中"零位姿态过近"要靠默认姿势/关节限位解决
4. 出正式工程图；动态预览（关节滑条 + 极限姿态扫掠）放在布局冻结之后

---

## 七、留给自己和后人的规矩

1. **参数必须有出处。** 没有出处的数不是数，是债。
2. **`unknown` 拦截构建。** 让错误发生在构建时，而不是在赛场上。
3. **推翻自己的结论时，把推翻过程留在文档里。** 不删、不盖、注明日期。
4. **仿真是仿真，实测是实测。** 两个词不许混用。
5. **取不到 ≠ 不存在。** 超时、403、404 都要复测再下结论。
6. **先做便宜的验证。** 90 元的舵机实测，优先级高于 4000 元的整机决策。

---

## 八、第一次多线程并行：6 条线程的账（2026-09-12）

一次派 6 条线程并行，是这轮最大的组织动作，也是第一次翻车——**账面如下，不美化**：

| 线程 | 干了什么 | 结果 |
|---|---|---|
| 过期口径清扫（glm-5.3-flash） | 全仓巡查过期数字 | ✅ 149 行报告，查出 11 项高优先级；随后 9/10 已改并留档 |
| 姿态扫掠（kimi-k2.7-code） | 极限姿态自碰撞 | ✅ `sweep_check.py`（自带 15 min 全局超时） |
| 螺钉可达性（minimax-m3） | 批头/手柄能否够到每个螺钉 | ✅ `tool_access.py` |
| 参考机反查（deepseek-v4-flash） | 参考机配合尺寸反查 | ✅ `reference_fits.py` |
| 舵机座让位（deepseek-v4-pro） | 三个大件的舵机让位切口 | ⚠️ 改动已落盘且实测有效（干涉 −27%），**但没写报告** |
| 真机总线驱动（grok-4.6 @ xbcl） | 串口驱动 + 测试 + 固件说明 | ❌ **零产出**（后被快路由重做，63 项测试全绿） |

**为什么当时看起来"全部卡住"**（三条硬证据，不猜）：

1. Grok 路由实测**首字节 25–75 秒**：`/v1/models` 秒回，但一次对话先 25 s 超时、重试才在 75 s 内返回。
   写一个模块要几十次请求 → 十几分钟起步，而它看起来和"死了"没有区别。
2. 工作流是**前台阻塞、又不显示单代理进度**的：慢线程在跑时界面就是静止的；
   一取消，子代理被整体拆掉（`list_agents` 变空），半成品连进程一起消失。
3. `tool_access.py` 当时没写 `--help`，任何"冒烟测试"都会触发整机装配 + 每颗螺钉的 OCCT 距离计算——
   分钟级、零输出，外观与卡死完全一致。

**回收**：4 条跑完的产物全部有效并已收编（`b0b6221`）；半成品那条的改动实测把干涉从
266 134 压到 **193 677 mm³**。**没有任何成果因为"看起来卡住"而丢失**——因为规矩要求线程把
成果落成文件，而不是留在对话里。

**因此立下的并行施工五条规矩**：
① 一个提交者（主会话），其余线程只写文件、不动 git 历史；
② 文件所有权互斥，禁止两条线程碰同一个文件；
③ 每条线程必写 `design/handoff/线程报告-<线程名>.md`，**附命令与真实输出**（报告即跨线程通信）；
④ **重 CAD 一次只开一条线**——本轮 6 条里 3 条要跑多分钟 OCCT，互相抢 CPU 本身也是"卡住"的来源；
⑤ 慢路由只派短刀任务（评审、单点判断），整模块写作走快路由。

---

## 附录：证据在哪

| 想核对什么 | 去哪看 |
|---|---|
| 全部提交与时间 | `git log --date=format:'%m-%d %H:%M' --pretty='%h \| %ad \| %s'` |
| 舵机接口核验（第 3 案） | `design/handoff/STS3215-机械接口核验.md` + `design/cad/tools/measure_servo.py` |
| 官方规格书与判据翻转（第 2 案） | `design/handoff/STS3215-官方规格书核验.md` + `design/cad/vendor/feetech/` |
| 参考机对标与第 1 案订正 | ⚠️ 原 `design/handoff/TonyPi-Pro-参考构型与零件参数.md` 已随第 6–10 轮文档重构删除；可从 `bfb5949` 恢复：`git show bfb5949:design/handoff/TonyPi-Pro-参考构型与零件参数.md` |
| 成本与质量困局 | `design/handoff/成本估算.md` |
| 赛题逐条自检 | ⚠️ 原 `design/handoff/第3轮-T6-小人形组合规核对.md` 已随重构删除，**内容未并入任何新文件**（待决定是否恢复）；赛题附件见 `docs/contest/` |
| 商用平台对照 | ⚠️ 原 `design/handoff/商用平台参数对标.md` 同上（已删除、未并入、可从 `bfb5949` 恢复） |
| 跑一遍测试 | `cd software/atri && python3 -m unittest discover -s tests`（venv 仍在旧路径 `软件/atri/.venv`，已在 `software/atri/.venv` 建软链接） |
| 跑一遍无硬件演示 | `cd software/atri && python3 run_demo.py --fast` |
| 干涉分类与切口定位（第 4 案） | `design/cad/pair_inspect.py` + `design/handoff/装配一致性修正记录.md` §6/§7 |
| 整机体检（干涉/对齐/连通） | `design/cad/audit_assembly.py --top 30` |
| 布局门禁（第 6–10 轮新增） | `design/cad/test_layout.py`（15 项） |
| 骨架重构评估（组会材料） | `docs/process/骨架重构评估-组会汇报.md` + `docs/process/cad-compare/` |
| 文档与赛题附件索引 | `docs/README.md`（`docs/process` / `docs/research/项目文档` / `docs/contest`） |
| 过期口径巡查与修正 | `design/handoff/线程报告-过期口径清扫.md` + `design/handoff/过期口径修正记录.md` |
| 电源预算被额定订正掀翻 | `design/handoff/过期口径修正记录.md` §功率 + `design/handoff/hardware_requirements.json` |
| 真机链路（驱动/接线/标定） | `design/handoff/线程报告-真机总线驱动.md` + `固件/README.md` + `design/reference/sts3215/PROTOCOL.md` |
| 并行施工与线程报告（§八） | `design/handoff/多线程并行施工方案.md` + `design/handoff/线程报告-*.md` |
