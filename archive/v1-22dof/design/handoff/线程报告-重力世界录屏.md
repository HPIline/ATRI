# 线程报告：带重力世界可复现生成与录屏清单（S5，2026-09-13）

> 执行者：DSH 子会话（第 12 轮 S5）
> 任务书：`design/handoff/第12轮-成熟度差距与多线程Prompt.md` S5
> 依据：`webots/README.md`「带重力/带地面的校核跑法」、
> `design/handoff/仿真请求-Webots验证清单.md` S1/S2/S5、
> `docs/contest/提交材料清单.md` §二
>
> ⚠ **本机（Mac）没有 Webots**（`command -v webots` 为空）。本线程只做
> **生成 + 文本 grep 自检 + 文档**。S1 静立 / S2 扭矩探针 / T-05 舞蹈 / 录屏 / 压码
> **全部未在真 Webots 里跑过**，命令写给队友。

---

## 0. 一句话结论

1. **生成成功**。包装脚本 `python3 webots/tools/make_gravity_world.py` 退出码 **0**。
2. 派生世界路径：`docs/process/sim/worlds/atri_22dof_gravity.wbt`（25609 字节）。
3. grep 三点全过：`gravity -9.81` × **1**、`maxTorque 2.94` × **22**、`Plane` × **3**（`Floor` × 0）。
4. **默认世界未被改写**：`webots/worlds/atri_22dof.wbt` 仍是 `gravity 0`、无 `maxTorque`、无 `Plane`。
5. 生成器 `webots/tools/generate_atri_world.py` **默认值未改**（`GRAVITY = 0.0` / `DEFAULT_MAX_TORQUE = None` / `DEFAULT_GROUND = False`）。
6. 录屏步骤在 `docs/process/sim/重力世界录屏脚本.md`。本机无 GUI，**没有录到任何 mp4**。

**口径（必须写进材料）**：只有这份带重力世界才能谈「站得住」。
默认 `webots/worlds/atri_22dof.wbt` 是零重力运动学联调世界，悬浮、静力矩 ≈ 0，
用它录的「静立」不能进答辩、不能写成仿真证明站立。

---

## 1. 本线程动过 / 没动过的文件

| 路径 | 状态 | 说明 |
|---|---|---|
| `webots/tools/make_gravity_world.py` | **新建** | 调用现有生成器 + 三条 grep + 拒写默认世界 |
| `docs/process/sim/重力世界录屏脚本.md` | **新建** | S1 / S2 / T-05 怎么启动、怎么录、怎么压到 720p/≤100 MB |
| `docs/process/sim/worlds/atri_22dof_gravity.wbt` | **生成产物** | 派生世界，未覆盖默认世界 |
| `design/handoff/线程报告-重力世界录屏.md` | **新建** | 本文件 |
| `webots/worlds/atri_22dof.wbt` | **未改** | 仍 `gravity 0` |
| `webots/tools/generate_atri_world.py` | **未改** | 默认值未动 |
| `.gitignore` | **未改** | 建议见 §6，留给主会话 |

Git：本线程**没有**执行 `git add/commit/push/checkout`。只读看过 `git status --porcelain`。

---

## 2. 生成命令与真实输出

仓库根目录、2026-09-13 约 09:34 实跑：

```bash
python3 webots/tools/make_gravity_world.py
```

内部调用（脚本打印的原文）：

```
/Library/Developer/CommandLineTools/usr/bin/python3 \
  /Users/zhangjingkun/Projects/github/ATRI/webots/tools/generate_atri_world.py \
  --gravity=-9.81 --max-torque=2.94 --ground \
  --out /Users/zhangjingkun/Projects/github/ATRI/docs/process/sim/worlds/atri_22dof_gravity.wbt
```

生成器 stdout（原文）：

```
  模型 v3.0，整机 3.037 kg，包络 372.8×190.0×123.0 mm
  [生成器] 非默认参数：gravity=-9.81 m/s²，maxTorque=2.94 N·m × 22，地面=有（Plane 4×4 m）
已生成 /Users/zhangjingkun/Projects/github/ATRI/docs/process/sim/worlds/atri_22dof_gravity.wbt
关节数 22: trunk_roll, trunk_pitch, head_yaw, head_pitch, left_shoulder_pitch, left_shoulder_roll, left_elbow_pitch, left_gripper, right_shoulder_pitch, right_shoulder_roll, right_elbow_pitch, right_gripper, left_hip_yaw, left_hip_roll, left_hip_pitch, left_knee_pitch, left_ankle_pitch, right_hip_yaw, right_hip_roll, right_hip_pitch, right_knee_pitch, right_ankle_pitch
```

包装脚本自检（原文）：

```
自检 grep（与 webots/README.md「带重力/带地面的校核跑法」同一套）：
  grep -c "gravity -9.81"  docs/process/sim/worlds/atri_22dof_gravity.wbt   → 1   （期望 1）
  grep -c "maxTorque 2.94" docs/process/sim/worlds/atri_22dof_gravity.wbt   → 22  （期望恰好 22）
  grep -cE "Floor|Plane"    docs/process/sim/worlds/atri_22dof_gravity.wbt   → Plane=3 Floor=0  （期望 ≥1）

自检通过。
文件大小: 25609 字节
默认世界仍是 gravity 0: 是
```

**退出码 0**。生成未失败，无报错可贴。

> 注：生成器打印的整机质量是 **3.037 kg**。T6 线程报告（2026-09-12）里曾是 3.137 kg。
> 这是**当前 `design/robot_model.json` 的派生结果**，不是本线程改质量。
> 仿真报告引用质量时以本次生成器输出 / 模型文件为准。

---

## 3. 关键 grep（手工复核，与脚本一致）

```bash
W=docs/process/sim/worlds/atri_22dof_gravity.wbt
grep -c "gravity -9.81" "$W"     # → 1
grep -c "maxTorque 2.94" "$W"    # → 22
grep -cE "Floor|Plane" "$W"      # → 3
grep -c "Plane" "$W"             # → 3
grep -c "Floor" "$W"             # → 0
```

| 项 | 结果 | 期望 | 判定 |
|---|---|---|---|
| `gravity -9.81` | **1** | 1 | 通过（文件头注释 1 处 + `WorldInfo` 1 处里，字符串 `gravity -9.81` 只在 WorldInfo 命中 1 次；注释行是 `gravity   = -9.81`，空格不同，不计入这条） |
| `maxTorque 2.94` | **22** | 恰好 22 | 通过（22 个 `RotationalMotor` 各一条） |
| `Floor\|Plane` | **3** | ≥1 | 通过（视觉 `geometry Plane` + `boundingObject Plane` + 文件头注释 "Plane"） |

默认世界对照：

```
webots/worlds/atri_22dof.wbt
  第 11 行: gravity 0
  maxTorque 命中: 0
  Plane 命中: 0
  文件大小: 24156 字节（2026-09-12 16:38，本线程未写）
```

世界文件头（派生世界原文摘录）：

```
WorldInfo {
  title "A.T.R.I. 22 DOF 桌面人形（带重力校核）"
  basicTimeStep 32
  gravity -9.81
  ...
}
Solid {
  name "ground"
  ...
  geometry Plane { size 4 4 }
  boundingObject Plane { size 4 4 }
}
Robot {
  name "ATRI"
  controller "atri_controller"
  translation 0 0 0.1942
```

---

## 4. 包装脚本行为（给主会话核对）

`webots/tools/make_gravity_world.py`：

- 不改生成器默认值，只 subprocess 调
  `--gravity=-9.81 --max-torque=2.94 --ground --out <派生路径>`。
- 输出目录不存在会创建（`docs/process/sim/worlds/`）。
- `--out` 若解析后等于 `webots/worlds/atri_22dof.wbt` → 拒绝，退出 2。
- 生成后检查默认世界仍含 `\n  gravity 0\n` 且不含 `gravity -9.81`。
- 打印给队友的 S1 / S2 / T-05 启动命令（本机无 Webots，只是文本）。

---

## 5. 录屏文档覆盖了什么

`docs/process/sim/重力世界录屏脚本.md`：

| 段 | 内容 |
|---|---|
| §0 | **只有带重力世界才能谈站得住**；本机可能无 GUI；平台 ≤100 MB / 建议 ≤3 min |
| §1 | 生成与三条 grep |
| §2 | 控制器发现：派生世界请先 `cp` 到 `webots/worlds/` 再打开（运行副本不提交） |
| §4 | S1 静立 10 s：空任务卡目录 + `webots --mode=realtime`；不要拿 batch 退出码判定 |
| §5 | S2：运行副本里把 `controller` 改成已有的 `torque_probe`；JSON 写到 `docs/process/sim/s2_torque_peak.json` |
| §6 | T-05：单独目录只放 `T-05_dance.json`，带重力世界录 20–30 s |
| §7 | 压到 720p30 @ 4 Mbps H.264（ffmpeg / avconvert / imageio-ffmpeg / HandBrake） |
| §10 | 一页纸命令给队友复制 |

硬要求已写进文档：

- 录屏用 `--mode=realtime`，**不要** `--mode=fast`。
- 摔倒就照实写「仿真值，带重力未站稳」，**不要切回零重力世界把动作录漂亮**。
- 路线 B（CAD 渲染 mp4）不能替代本清单，不能证明动力学。

---

## 6. 给主会话：`.gitignore` 建议（本线程未改）

当前 `.gitignore` **没有**忽略 `docs/process/sim/worlds/`。
`git status --porcelain` 里派生世界是未跟踪文件。

建议主会话二选一（不要让子线程改 `.gitignore`）：

**A. 当生成产物忽略（推荐，和 README「不要覆盖默认世界 / 生成产物」一致）：**

```
# 带重力校核世界与录屏产物（python3 webots/tools/make_gravity_world.py 可复现）
docs/process/sim/worlds/
docs/process/sim/empty_cards/
docs/process/sim/cards_t05/
docs/process/sim/video/
docs/process/sim/*.json
webots/worlds/atri_22dof_gravity.wbt
```

**B. 把 25 KB 的 `docs/process/sim/worlds/atri_22dof_gravity.wbt` 入库**，
方便没有 Python/模型链的队友直接打开；仍应忽略
`webots/worlds/atri_22dof_gravity.wbt`（运行副本，以免有人误提交、CI 也扫不到它）。

无论 A 还是 B，**禁止**把运行副本提交成默认世界，也禁止改 `atri_22dof.wbt`。

---

## 7. 诚实边界 / 没做的事

| 项 | 状态 |
|---|---|
| 派生世界文本生成 | ✅ 本机实跑，退出 0 |
| 三条 grep | ✅ 1 / 22 / 3 |
| 默认世界仍 gravity 0 | ✅ |
| 在 Webots 里打开 | ❌ 本机无 `webots` |
| S1 10 s 静立是否翻倒 | ❌ 未实测 |
| S2 `getTorqueFeedback` 数字 | ❌ 未实测 |
| T-05 带重力是否站得住 | ❌ 未实测 |
| 录屏 / 压码出 mp4 | ❌ 无 GUI、无素材 |
| `basicTimeStep 32` 接触是否抖 | ❌ 未实测；文档写了可改 8 |
| 脚碰撞体 | 仍是盒子，不是 CAD 四只 Φ8 垫 |
| 控制器在 `docs/process/sim/` project 下能否发现 | README 已警告，**未实测** |

S1/S2/S5 视频仍依赖**队友机器上的 Webots GUI**。本线程只把「生成世界 + 可复制命令」闭合了。

---

## 8. 口径声明

- 本报告中的**真实输出**：§2 命令与 stdout、§3 grep 计数，2026-09-13 本机实跑。
- 本报告中的**静态核实值**：`gravity -9.81`、`maxTorque 2.94` × 22、`Plane` 4×4 m——都是对 `.wbt` 文本的 grep，不是 Webots 运行时读数。
- 本报告**不含实测值**，也不含「站得住」的仿真结论（仿真还没跑）。
- 设计值未在本线程重测：STS3215 堵转 2.94 N·m、连续额定 0.98 N·m，见 `hardware_requirements.json`。
